---
description: Ошибка компилятора CS1708
title: Ошибка компилятора CS1708
ms.date: 07/20/2015
f1_keywords:
- CS1708
helpviewer_keywords:
- CS1708
ms.assetid: df2fc580-2de5-4dd0-9ba3-b41248de5f41
ms.openlocfilehash: 3ffb953c97aff7206dff7ca6fb3bcdc9f95dc137
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: HT
ms.contentlocale: ru-RU
ms.lasthandoff: 09/24/2020
ms.locfileid: "91196927"
---
# <a name="compiler-error-cs1708"></a><span data-ttu-id="f258e-103">Ошибка компилятора CS1708</span><span class="sxs-lookup"><span data-stu-id="f258e-103">Compiler Error CS1708</span></span>

<span data-ttu-id="f258e-104">Доступ к буферам фиксированного размера разрешен только через локальные переменные или поля</span><span class="sxs-lookup"><span data-stu-id="f258e-104">Fixed size buffers can only be accessed through locals or fields</span></span>  
  
 <span data-ttu-id="f258e-105">Новая функция в C# 2.0 — способность определять встроенный массив внутри `struct`.</span><span class="sxs-lookup"><span data-stu-id="f258e-105">A new feature in C# 2.0 is the ability to define an in-line array inside of a `struct`.</span></span> <span data-ttu-id="f258e-106">Доступ к таким массивам может осуществляться только через локальные переменные или поля, и ссылаться на них как на промежуточные значения слева от выражения нельзя.</span><span class="sxs-lookup"><span data-stu-id="f258e-106">Such arrays can only be accessed via local variables or fields, and may not be referenced as intermediate values on the left-hand side of an expression.</span></span> <span data-ttu-id="f258e-107">Кроме того, доступ к массивам нельзя получить через поля, которые являются `static` или `readonly`.</span><span class="sxs-lookup"><span data-stu-id="f258e-107">Also, the arrays cannot be accessed by fields that are `static` or `readonly`.</span></span>  
  
 <span data-ttu-id="f258e-108">Чтобы устранить эту ошибку, определите переменную массива и присвойте встроенный массив переменной.</span><span class="sxs-lookup"><span data-stu-id="f258e-108">To resolve this error, define an array variable, and assign the in-line array to the variable.</span></span> <span data-ttu-id="f258e-109">Кроме того, можно удалить модификатор `static` или `readonly` из поля, представляющего встроенный массив.</span><span class="sxs-lookup"><span data-stu-id="f258e-109">Or, remove the `static` or `readonly` modifier from the field representing the in-line array.</span></span>  
  
## <a name="example"></a><span data-ttu-id="f258e-110">Пример</span><span class="sxs-lookup"><span data-stu-id="f258e-110">Example</span></span>  

 <span data-ttu-id="f258e-111">В следующем примере возникает ошибка CS1708.</span><span class="sxs-lookup"><span data-stu-id="f258e-111">The following sample generates CS1708.</span></span>  
  
```csharp  
// CS1708.cs  
// compile with: /unsafe  
using System;  
  
unsafe public struct S  
{  
    public fixed char name[10];  
}  
  
public unsafe class C  
{  
    public S UnsafeMethod()  
    {  
        S myS = new S();  
        return myS;  
    }  
  
    static void Main()  
    {  
        C myC = new C();  
        myC.UnsafeMethod().name[3] = 'a';  // CS1708  
        // Uncomment the following 2 lines to resolve:  
        // S myS = myC.UnsafeMethod();  
        // myS.name[3] = 'a';  
  
        // The field cannot be static.  
        C._s1.name[3] = 'a';  // CS1708  
  
        // The field cannot be readonly.  
        myC._s2.name[3] = 'a';  // CS1708  
    }  
  
    static readonly S _s1;  
    public readonly S _s2;  
}  
```
