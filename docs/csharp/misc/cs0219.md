---
description: Предупреждение компилятора (уровень 3) CS0219
title: Предупреждение компилятора (уровень 3) CS0219
ms.date: 09/06/2019
f1_keywords:
- CS0219
helpviewer_keywords:
- CS0219
ms.assetid: 7945c497-4bfa-4695-9166-815a2ad0c8e7
ms.openlocfilehash: 67d8f027c4e4fdd33fa44ed85eb3fc0b8319a917
ms.sourcegitcommit: d579fb5e4b46745fd0f1f8874c94c6469ce58604
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 08/30/2020
ms.locfileid: "89124231"
---
# <a name="compiler-warning-level-3-cs0219"></a><span data-ttu-id="ba995-103">Предупреждение компилятора (уровень 3) CS0219</span><span class="sxs-lookup"><span data-stu-id="ba995-103">Compiler Warning (level 3) CS0219</span></span>
<span data-ttu-id="ba995-104">Переменной "переменная" присвоено значение, но оно ни разу не использовалось</span><span class="sxs-lookup"><span data-stu-id="ba995-104">The variable 'variable' is assigned but its value is never used</span></span>

 <span data-ttu-id="ba995-105">Если переменная объявлена и ей присвоено значение, но оно не используется, компилятор выдает предупреждение третьего уровня.</span><span class="sxs-lookup"><span data-stu-id="ba995-105">The compiler issues a level-three warning, when you declare and assign a variable, but do not use it.</span></span>

 > [!NOTE]
 > <span data-ttu-id="ba995-106">Компилятор создает это предупреждение, только если значение переменной является константой во время компиляции.</span><span class="sxs-lookup"><span data-stu-id="ba995-106">The compiler generates this warning only when the variable value is a compile-time constant.</span></span> <span data-ttu-id="ba995-107">Присвоение неконстантного выражения или результата метода локальной переменной упрощает просмотр этих выражений в отладчике.</span><span class="sxs-lookup"><span data-stu-id="ba995-107">Assigning a non-constant expression or method result to a local variable makes it easier to observe those expressions in the debugger.</span></span> <span data-ttu-id="ba995-108">Это также делает результат достижимым, предотвращая сбор мусора, пока эта переменная достижима.</span><span class="sxs-lookup"><span data-stu-id="ba995-108">It also makes the result reachable, preventing garbage collection while that variable is reachable.</span></span>

 <span data-ttu-id="ba995-109">В следующем примере показаны случаи, когда и когда не создается предупреждение:</span><span class="sxs-lookup"><span data-stu-id="ba995-109">The following sample shows the cases when and when not the warning is generated:</span></span>

```csharp
// CS0219.cs
// compile with: /W:3
public class MyClass
{
   public static void Main()
   {
      var interpolated = "Interpolated";
      var a = 0; // CS0219
      int b = GetZero(); // Doesn't generate a warning.
      var c = "Regular string"; // CS0219
      var d = $"Constant interpolated string"; // Doesn't generate a warning.
      var e = $"{interpolated} string"; // Doesn't generate a warning.
   }

   private static int GetZero()
   {
       return 0;
   }
}  
```
