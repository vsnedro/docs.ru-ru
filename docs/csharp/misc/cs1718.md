---
title: Предупреждение компилятора (уровень 3) CS1718
ms.date: 07/20/2015
f1_keywords:
- CS1718
helpviewer_keywords:
- CS1718
ms.assetid: 7c1c11fd-4f91-482d-b8f7-efe2a361634e
ms.openlocfilehash: debbe75d3464a0b2e08ae088f6ce25648c9f29be
ms.sourcegitcommit: f87ad41b8e62622da126aa928f7640108c4eff98
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/07/2020
ms.locfileid: "80805871"
---
# <a name="compiler-warning-level-3-cs1718"></a><span data-ttu-id="9d64d-102">Предупреждение компилятора (уровень 3) CS1718</span><span class="sxs-lookup"><span data-stu-id="9d64d-102">Compiler Warning (level 3) CS1718</span></span>
<span data-ttu-id="9d64d-103">Сравнение выполнено с той же переменной. Действительно следует выполнять такое сравнение?</span><span class="sxs-lookup"><span data-stu-id="9d64d-103">Comparison made to same variable; did you mean to compare something else?</span></span>  
  
 <span data-ttu-id="9d64d-104">Если вы хотели сравнить с чем-то другим, то исправьте заявление.</span><span class="sxs-lookup"><span data-stu-id="9d64d-104">If you meant to compare to something else, then correct the statement.</span></span>  
  
 <span data-ttu-id="9d64d-105">Однако может быть и так, что вы выполняли проверку на возвращение значения true или false и для этого использовали такие операторы, как `if (a == a) (true)` или `if (a < a) (false)`.</span><span class="sxs-lookup"><span data-stu-id="9d64d-105">But another possibility is that you were testing for true or false, and were doing so by statements such as `if (a == a) (true)` or `if (a < a) (false)`.</span></span> <span data-ttu-id="9d64d-106">Лучше использовать `if (true)` или, `if (false)`по следующим двум причинам:</span><span class="sxs-lookup"><span data-stu-id="9d64d-106">It is better to use `if (true)` or `if (false)`, for the following two reasons:</span></span>  
  
- <span data-ttu-id="9d64d-107">Последние операторы проще: всегда лучше написать ровно столько, сколько нужно.</span><span class="sxs-lookup"><span data-stu-id="9d64d-107">It is simpler: it is always clearer to simply say what you mean.</span></span>  
  
- <span data-ttu-id="9d64d-108">Использование этих операторов позволяет избежать неоднозначности: в версии C# 2.0 введена новая функция — типы значений, допускающие значение NULL, которые являются аналогом значения `null` в T-SQL — языке программирования, используемом в SQL Server.</span><span class="sxs-lookup"><span data-stu-id="9d64d-108">It helps avoid confusion: a new feature of C# 2.0 is nullable value types, which are analogous to the value `null` in T-SQL, the programming language used by SQL Server.</span></span> <span data-ttu-id="9d64d-109">Разработчики, знакомые с T-S'L, могут быть обеспокоены `if (a == a)`влиянием недействительных типов значений на такие выражения, как, из-за использования тернариной логики в T-S'L.</span><span class="sxs-lookup"><span data-stu-id="9d64d-109">Developers familiar with T-SQL might be concerned about the effect of nullable value types on expressions such as `if (a == a)`, because of the use of ternary logic in T-SQL.</span></span> <span data-ttu-id="9d64d-110">При использовании значения `true` или `false`можно устранить возможную неоднозначность.</span><span class="sxs-lookup"><span data-stu-id="9d64d-110">If you use `true` or `false`, you avoid this possible confusion.</span></span>  
  
## <a name="example"></a><span data-ttu-id="9d64d-111">Пример</span><span class="sxs-lookup"><span data-stu-id="9d64d-111">Example</span></span>  
 <span data-ttu-id="9d64d-112">Приведенный ниже пример кода приводит к возникновению предупреждения CS1718.</span><span class="sxs-lookup"><span data-stu-id="9d64d-112">The following code generates warning CS1718.</span></span>  
  
```csharp  
// CS1718.cs  
using System;  
public class Tester
{  
    public static void Main()
    {
        int i = 0;  
        if (i == i) { // CS1718.cs  
        //if (true) {
            i++;  
        }  
    }  
}  
```
