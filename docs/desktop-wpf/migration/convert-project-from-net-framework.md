---
title: Миграция WPF Приложения на .NET Core 3.0
description: Узнайте, как перенести приложение Windows Presentation Foundation (WPF) в .NET Core 3.0.
author: mjrousos
ms.date: 09/12/2019
ms.author: mikerou
ms.openlocfilehash: f52005e7c8a6312b8c4e09a950f1f635af1894e4
ms.sourcegitcommit: cdf5084648bf5e77970cbfeaa23f1cab3e6e234e
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 02/01/2020
ms.locfileid: "81432596"
---
# <a name="migrating-wpf-apps-to-net-core"></a><span data-ttu-id="f33fd-103">Миграция приложений WPF в ядро .NET</span><span class="sxs-lookup"><span data-stu-id="f33fd-103">Migrating WPF apps to .NET Core</span></span>

<span data-ttu-id="f33fd-104">В этой статье рассматриваются шаги, необходимые для переноса приложения Windows Presentation Foundation (WPF) из рамочного .NET в .NET Core 3.0.</span><span class="sxs-lookup"><span data-stu-id="f33fd-104">This article covers the steps necessary to migrate a Windows Presentation Foundation (WPF) app from .NET Framework to .NET Core 3.0.</span></span> <span data-ttu-id="f33fd-105">Если у вас нет приложения WPF под рукой, чтобы порт, но хотели бы опробовать процесс, вы можете использовать образец приложения **Bean Trader,** доступный на [GitHub.](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader)</span><span class="sxs-lookup"><span data-stu-id="f33fd-105">If you don't have a WPF app on hand to port, but would like to try out the process, you can use the **Bean Trader** sample app available on [GitHub](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader).</span></span> <span data-ttu-id="f33fd-106">Оригинальное приложение (таргетинг .NET Framework 4.7.2) доступно в папке NetFx-BeanTraderClient.</span><span class="sxs-lookup"><span data-stu-id="f33fd-106">The original app (targeting .NET Framework 4.7.2) is available in the NetFx\BeanTraderClient folder.</span></span> <span data-ttu-id="f33fd-107">Сначала мы объясним шаги, необходимые для переноса приложений в целом, а затем мы пройдемся по конкретным изменениям, которые применяются к образцу **Bean Trader.**</span><span class="sxs-lookup"><span data-stu-id="f33fd-107">First we'll explain the steps necessary to port apps in general, and then we'll walk through the specific changes that apply to the **Bean Trader** sample.</span></span>

[!INCLUDE [desktop guide under construction](../../../includes/desktop-guide-preview-note.md)]

<span data-ttu-id="f33fd-108">Чтобы перейти на ядро .NET, необходимо сначала:</span><span class="sxs-lookup"><span data-stu-id="f33fd-108">To migrate to .NET Core, you must first:</span></span>

01. <span data-ttu-id="f33fd-109">Понимание и обновление зависимостей NuGet:</span><span class="sxs-lookup"><span data-stu-id="f33fd-109">Understand and update NuGet dependencies:</span></span>

    01. <span data-ttu-id="f33fd-110">Обновление зависимостей NuGet `<PackageReference>` для использования формата.</span><span class="sxs-lookup"><span data-stu-id="f33fd-110">Upgrade NuGet dependencies to use the `<PackageReference>` format.</span></span>
    01. <span data-ttu-id="f33fd-111">Просмотрите зависимость на высшем уровне NuGet для совместимости .NET Core или .NET Standard.</span><span class="sxs-lookup"><span data-stu-id="f33fd-111">Review top-level NuGet dependencies for .NET Core or .NET Standard compatibility.</span></span>
    01. <span data-ttu-id="f33fd-112">Обновление пакетов NuGet до новых версий.</span><span class="sxs-lookup"><span data-stu-id="f33fd-112">Upgrade NuGet packages to newer versions.</span></span>
    01. <span data-ttu-id="f33fd-113">Используйте [анализатор портативности .NET,](../../standard/analyzers/portability-analyzer.md) чтобы понять зависимости .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-113">Use the [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) to understand .NET dependencies.</span></span>

01. <span data-ttu-id="f33fd-114">Перевежните файл проекта в новый формат sDK:</span><span class="sxs-lookup"><span data-stu-id="f33fd-114">Migrate the project file to the new SDK-style format:</span></span>

    01. <span data-ttu-id="f33fd-115">Выберите, таргетировать ли основное ядро .NET и рамки .NET, или только на ядро .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-115">Choose whether to target both .NET Core and .NET Framework, or only .NET Core.</span></span>
    01. <span data-ttu-id="f33fd-116">Копирование соответствующих свойств файлов проекта и элементов в новый файл проекта.</span><span class="sxs-lookup"><span data-stu-id="f33fd-116">Copy relevant project file properties and items to the new project file.</span></span>

01. <span data-ttu-id="f33fd-117">Исправление проблем со сборкой:</span><span class="sxs-lookup"><span data-stu-id="f33fd-117">Fix build issues:</span></span>

    01. <span data-ttu-id="f33fd-118">Добавьте ссылку на пакет [Microsoft.Windows.Compatibility.](https://www.nuget.org/packages/Microsoft.Windows.Compatibility/)</span><span class="sxs-lookup"><span data-stu-id="f33fd-118">Add a reference to the [Microsoft.Windows.Compatibility](https://www.nuget.org/packages/Microsoft.Windows.Compatibility/) package.</span></span>
    01. <span data-ttu-id="f33fd-119">Найдите и исправьте различия уровня API.</span><span class="sxs-lookup"><span data-stu-id="f33fd-119">Find and fix API-level differences.</span></span>
    01. <span data-ttu-id="f33fd-120">Удалите разделы *app.config,* кроме `appSettings` или `connectionStrings`.</span><span class="sxs-lookup"><span data-stu-id="f33fd-120">Remove *app.config* sections other than `appSettings` or `connectionStrings`.</span></span>
    01. <span data-ttu-id="f33fd-121">При необходимости регенерировать генерируемый код.</span><span class="sxs-lookup"><span data-stu-id="f33fd-121">Regenerate generated code, if necessary.</span></span>

01. <span data-ttu-id="f33fd-122">Тестирование времени выполнения:</span><span class="sxs-lookup"><span data-stu-id="f33fd-122">Runtime testing:</span></span>

    01. <span data-ttu-id="f33fd-123">Подтвердите, что портированное приложение работает как ожидалось.</span><span class="sxs-lookup"><span data-stu-id="f33fd-123">Confirm the ported app works as expected.</span></span>
    01. <span data-ttu-id="f33fd-124">Остерегайтесь исключений. <xref:System.NotSupportedException></span><span class="sxs-lookup"><span data-stu-id="f33fd-124">Beware of <xref:System.NotSupportedException> exceptions.</span></span>

## <a name="about-the-sample"></a><span data-ttu-id="f33fd-125">Сведения о примере</span><span class="sxs-lookup"><span data-stu-id="f33fd-125">About the sample</span></span>

<span data-ttu-id="f33fd-126">В этой статье содержится ссылка на [пример приложения Bean Trader,](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader) поскольку оно использует различные зависимости, аналогичные тем, которые могут иметь реальные приложения WPF.</span><span class="sxs-lookup"><span data-stu-id="f33fd-126">This article references the [Bean Trader sample app](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader) because it uses a variety of dependencies similar to those that real-world WPF apps might have.</span></span> <span data-ttu-id="f33fd-127">Приложение не большой, но предназначен для шага вверх от "Hello World" с точки зрения сложности.</span><span class="sxs-lookup"><span data-stu-id="f33fd-127">The app isn't large, but is meant to be a step up from 'Hello World' in terms of complexity.</span></span> <span data-ttu-id="f33fd-128">Приложение демонстрирует некоторые проблемы, с которыми пользователи могут столкнуться при переносе реальных приложений.</span><span class="sxs-lookup"><span data-stu-id="f33fd-128">The app demonstrates some issues users may encounter while porting real apps.</span></span> <span data-ttu-id="f33fd-129">Приложение общается с службой WCF, так что для того, чтобы работать должным образом, вам также нужно запустить проект BeanTraderServer (доступно в том же репозитории GitHub) и убедитесь, что конфигурация BeanTraderClient указывает на правильную конечную точку.</span><span class="sxs-lookup"><span data-stu-id="f33fd-129">The app communicates with a WCF service, so for it to run properly, you'll also need to run the BeanTraderServer project (available in the same GitHub repository) and make sure the BeanTraderClient configuration points to the correct endpoint.</span></span> <span data-ttu-id="f33fd-130">(По умолчанию, образец предполагает, что сервер *http://localhost:8090*работает на той же машине, что будет верно, если вы запустите BeanTraderServer локально.)</span><span class="sxs-lookup"><span data-stu-id="f33fd-130">(By default, the sample assumes the server is running on the same machine at *http://localhost:8090*, which will be true if you launch BeanTraderServer locally.)</span></span>

<span data-ttu-id="f33fd-131">Имейте в виду, что этот пример приложения предназначен для демонстрации проблем и решений переноса .NET Core Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-131">Keep in mind that this sample app is meant to demonstrate .NET Core porting challenges and solutions.</span></span> <span data-ttu-id="f33fd-132">Она не предназначена для демонстрации передового опыта WPF.</span><span class="sxs-lookup"><span data-stu-id="f33fd-132">It's not meant to demonstrate WPF best practices.</span></span> <span data-ttu-id="f33fd-133">В самом деле, он намеренно включает в себя некоторые анти-шаблоны, чтобы убедиться, что вы столкнетесь по крайней мере пару интересных проблем при переносе.</span><span class="sxs-lookup"><span data-stu-id="f33fd-133">In fact, it deliberately includes some anti-patterns to make sure you come across at least a couple of interesting challenges while porting.</span></span>

## <a name="getting-ready"></a><span data-ttu-id="f33fd-134">Подготовка</span><span class="sxs-lookup"><span data-stu-id="f33fd-134">Getting ready</span></span>

<span data-ttu-id="f33fd-135">Основная проблема переноса приложения .NET Framework в ядро .NET заключается в том, что его зависимости могут работать по-другому или вообще не работать.</span><span class="sxs-lookup"><span data-stu-id="f33fd-135">The primary challenge of migrating a .NET Framework app to .NET Core is that its dependencies may work differently or not at all.</span></span> <span data-ttu-id="f33fd-136">Миграция намного проще, чем раньше; многие пакеты NuGet теперь нацелены на стандарт .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-136">Migration is much easier than it used to be; many NuGet packages now target .NET Standard.</span></span> <span data-ttu-id="f33fd-137">Начиная с .NET Core 2.0, поверхностные зоны .NET И.NET и .NET Core стали похожими.</span><span class="sxs-lookup"><span data-stu-id="f33fd-137">Starting with .NET Core 2.0, the .NET Framework and .NET Core surface areas have become similar.</span></span> <span data-ttu-id="f33fd-138">Несмотря на это, некоторые различия (как в поддержку пакетов NuGet, так и в доступных AIS .NET) остаются.</span><span class="sxs-lookup"><span data-stu-id="f33fd-138">Even so, some differences (both in support from NuGet packages and in available .NET APIs) remain.</span></span> <span data-ttu-id="f33fd-139">Первым шагом в миграции является проверка зависимостей приложения и обеспечение того, чтобы ссылки были в формате, который легко переносится в ядро .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-139">The first step in migrating is to review the app's dependencies and make sure references are in a format that's easily migrated to .NET Core.</span></span>

### <a name="upgrade-to-packagereference-nuget-references"></a><span data-ttu-id="f33fd-140">Обновление `<PackageReference>` до ссылок NuGet</span><span class="sxs-lookup"><span data-stu-id="f33fd-140">Upgrade to `<PackageReference>` NuGet references</span></span>

<span data-ttu-id="f33fd-141">Старые проекты .NET Framework обычно перечисляют свои зависимости NuGet в файле *packages.config.*</span><span class="sxs-lookup"><span data-stu-id="f33fd-141">Older .NET Framework projects typically list their NuGet dependencies in a *packages.config* file.</span></span> <span data-ttu-id="f33fd-142">Новый формат файла проекта в стиле SDK [`<PackageReference>`](/nuget/consume-packages/package-references-in-project-files) ссылается на пакеты NuGet как на элементы в самом файле csproj, а не в отдельном файле конфигурации.</span><span class="sxs-lookup"><span data-stu-id="f33fd-142">The new SDK-style project file format references NuGet packages as [`<PackageReference>`](/nuget/consume-packages/package-references-in-project-files) elements in the csproj file itself rather than in a separate config file.</span></span>

<span data-ttu-id="f33fd-143">При миграции есть два преимущества `<PackageReference>`использования ссылок в стиле:</span><span class="sxs-lookup"><span data-stu-id="f33fd-143">When migrating, there are two advantages to using `<PackageReference>`-style references:</span></span>

- <span data-ttu-id="f33fd-144">Это стиль ссылки NuGet, который необходим для нового файла проекта .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-144">This is the style of NuGet reference that is required for the new .NET Core project file.</span></span> <span data-ttu-id="f33fd-145">Если вы уже `<PackageReference>`используете, эти элементы файла проекта могут быть скопированы и вставлены непосредственно в новый проект.</span><span class="sxs-lookup"><span data-stu-id="f33fd-145">If you're already using `<PackageReference>`, those project file elements can be copied and pasted directly into the new project.</span></span>
- <span data-ttu-id="f33fd-146">В отличие от файла `<PackageReference>` packages.config, элементы относятся только к зависимостям верхнего уровня, от которых напрямую зависит ваш проект.</span><span class="sxs-lookup"><span data-stu-id="f33fd-146">Unlike a packages.config file, `<PackageReference>` elements only refer to the top-level dependencies that your project depends on directly.</span></span> <span data-ttu-id="f33fd-147">Все остальные транзитные пакеты NuGet будут определены во время восстановления и записаны в автогенерированном файле obj'project.assets.json.</span><span class="sxs-lookup"><span data-stu-id="f33fd-147">All other transitive NuGet packages will be determined at restore time and recorded in the autogenerated obj\project.assets.json file.</span></span> <span data-ttu-id="f33fd-148">Это значительно упрощает определение зависимостей проекта, что полезно при определении того, будут ли необходимые зависимости работать на .NET Core или нет.</span><span class="sxs-lookup"><span data-stu-id="f33fd-148">This makes it much easier to determine what dependencies your project has, which is useful when determining whether the necessary dependencies will work on .NET Core or not.</span></span>

<span data-ttu-id="f33fd-149">Первым шагом к переходу приложения .NET Framework в ядро .NET является обновление его для использования `<PackageReference>` ссылок NuGet.</span><span class="sxs-lookup"><span data-stu-id="f33fd-149">The first step to migrating a .NET Framework app to .NET Core is to update it to use `<PackageReference>` NuGet references.</span></span> <span data-ttu-id="f33fd-150">Visual Studio делает это простым.</span><span class="sxs-lookup"><span data-stu-id="f33fd-150">Visual Studio makes this simple.</span></span> <span data-ttu-id="f33fd-151">Просто право нажмите *на пакет проекта.конфигурация* файл в Visual Studio **Solution Explorer**, а затем выберите **Migrate packages.config к PackageReference**.</span><span class="sxs-lookup"><span data-stu-id="f33fd-151">Just right-click the project's *packages.config* file in Visual Studio's **Solution Explorer**, and then select **Migrate packages.config to PackageReference**.</span></span>

![Обновление до PackageReference](./media/convert-project-from-net-framework/package-reference-migration.png)

<span data-ttu-id="f33fd-153">Появляется диалог, показывающий рассчитанные зависимые от NuGet верхнего уровня и спрашивающий, какие другие пакеты NuGet следует продвигать на высший уровень.</span><span class="sxs-lookup"><span data-stu-id="f33fd-153">A dialog appears showing calculated top-level NuGet dependencies and asking which other NuGet packages should be promoted to top-level.</span></span> <span data-ttu-id="f33fd-154">Ни один из этих других пакетов не должен быть на высшем уровне для образца Bean Trader, так что вы можете отменить все эти коробки.</span><span class="sxs-lookup"><span data-stu-id="f33fd-154">None of these other packages need to be top-level for the Bean Trader sample, so you can uncheck all of those boxes.</span></span> <span data-ttu-id="f33fd-155">Затем нажмите **Ok** и файл *packages.config* удаляется и `<PackageReference>` элементы добавляются в файл проекта.</span><span class="sxs-lookup"><span data-stu-id="f33fd-155">Then, click **Ok** and the *packages.config* file is removed and `<PackageReference>` elements are added to the project file.</span></span>

<span data-ttu-id="f33fd-156">`<PackageReference>`- Ссылки в стиле не хранят пакеты NuGet локально в папке пакетов.</span><span class="sxs-lookup"><span data-stu-id="f33fd-156">`<PackageReference>`-style references don't store NuGet packages locally in a packages folder.</span></span> <span data-ttu-id="f33fd-157">Вместо этого они хранятся по всему миру в качестве оптимизации.</span><span class="sxs-lookup"><span data-stu-id="f33fd-157">Instead, they're stored globally as an optimization.</span></span> <span data-ttu-id="f33fd-158">После завершения миграции отойдите от файла `<Analyzer>` csproj и удалите все элементы, относящиеся к анализателям, которые ранее пришли из *.. Каталог пакетов.*</span><span class="sxs-lookup"><span data-stu-id="f33fd-158">After the migration completes, edit the csproj file and remove any `<Analyzer>` elements referring to the analyzers that previously came from the *..\packages* directory.</span></span> <span data-ttu-id="f33fd-159">Не волнуйтесь; так как у вас все еще есть ссылки на пакет NuGet, анализаторы будут включены в проект.</span><span class="sxs-lookup"><span data-stu-id="f33fd-159">Don't worry; since you still have the NuGet package references, the analyzers will be included in the project.</span></span> <span data-ttu-id="f33fd-160">Вам просто нужно очистить старые элементы в `<Analyzer>` стиле packages.config.</span><span class="sxs-lookup"><span data-stu-id="f33fd-160">You just need to clean up the old packages.config-style `<Analyzer>` elements.</span></span>

### <a name="review-nuget-packages"></a><span data-ttu-id="f33fd-161">Просмотр пакетов NuGet</span><span class="sxs-lookup"><span data-stu-id="f33fd-161">Review NuGet packages</span></span>

<span data-ttu-id="f33fd-162">Теперь, когда вы можете увидеть пакеты NuGet верхнего уровня, от которого зависит проект, можно просмотреть, доступны ли эти пакеты на .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-162">Now that you can see the top-level NuGet packages that the project depends on, you can review whether those packages are available on .NET Core.</span></span> <span data-ttu-id="f33fd-163">Вы можете определить, поддерживает ли пакет .NET Core, глядя на его зависимости от [nuget.org.](https://www.nuget.org/) Созданный сообществом [fuget.org](https://www.fuget.org/) сайт показывает эту информацию на видном месте в верхней части страницы информации пакета.</span><span class="sxs-lookup"><span data-stu-id="f33fd-163">You can determine whether a package supports .NET Core by looking at its dependencies on [nuget.org](https://www.nuget.org/). The community-created [fuget.org](https://www.fuget.org/) site shows this information prominently at the top of the package information page.</span></span>

<span data-ttu-id="f33fd-164">При таргетинге .NET Core 3.0 должны работать любые пакеты, ориентированные на .NET Core или .NET Standard (поскольку .NET Core реализует область поверхности .NET Standard).</span><span class="sxs-lookup"><span data-stu-id="f33fd-164">When targeting .NET Core 3.0, any packages targeting .NET Core or .NET Standard should work (since .NET Core implements the .NET Standard surface area).</span></span> <span data-ttu-id="f33fd-165">В некоторых случаях конкретная версия используемого пакета не будет нацелена на .NET Core или .NET Standard, но новые версии будут.</span><span class="sxs-lookup"><span data-stu-id="f33fd-165">In some cases, the specific version of a package that's used won't target .NET Core or .NET Standard, but newer versions will.</span></span> <span data-ttu-id="f33fd-166">В этом случае следует рассмотреть вопрос о переходе на последнюю версию пакета.</span><span class="sxs-lookup"><span data-stu-id="f33fd-166">In this case, you should consider upgrading to the latest version of the package.</span></span>

<span data-ttu-id="f33fd-167">Вы также можете использовать пакеты, таргетинг на .NET Framework, но это создает определенный риск.</span><span class="sxs-lookup"><span data-stu-id="f33fd-167">You can use packages targeting .NET Framework, as well, but that introduces some risk.</span></span> <span data-ttu-id="f33fd-168">"Ядро" для зависимостей .NET Framework разрешено, поскольку поверхностные области .NET Core и .NET Framework достаточно похожи, чтобы такие зависимости *часто* работали.</span><span class="sxs-lookup"><span data-stu-id="f33fd-168">.NET Core to .NET Framework dependencies are allowed because .NET Core and .NET Framework surface areas are similar enough that such dependencies *often* work.</span></span> <span data-ttu-id="f33fd-169">Однако, если пакет попытается использовать API .NET, который не присутствует в ядро .NET, вы столкнетесь с исключением времени выполнения.</span><span class="sxs-lookup"><span data-stu-id="f33fd-169">However, if the package tries to use a .NET API that isn't present in .NET Core, you'll encounter a runtime exception.</span></span> <span data-ttu-id="f33fd-170">Из-за этого следует ссылаться только на пакеты .NET Framework, когда нет других вариантов, и понимать, что это накладывает на тестовый груз.</span><span class="sxs-lookup"><span data-stu-id="f33fd-170">Because of that, you should only reference .NET Framework packages when no other options are available and understand that doing so imposes a test burden.</span></span>

<span data-ttu-id="f33fd-171">Если на них ссылаются пакеты, не нацеленные на .NET Core или .NET Standard, вам придется подумать о других альтернативах:</span><span class="sxs-lookup"><span data-stu-id="f33fd-171">If there are packages referenced that don't target .NET Core or .NET Standard, you'll have to think about other alternatives:</span></span>

- <span data-ttu-id="f33fd-172">Есть ли другие аналогичные пакеты, которые могут быть использованы вместо этого?</span><span class="sxs-lookup"><span data-stu-id="f33fd-172">Are there other similar packages that can be used instead?</span></span> <span data-ttu-id="f33fd-173">Иногда авторы NuGet публикуют отдельные '. Основные версии своих библиотек специально ориентированы на .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-173">Sometimes NuGet authors publish separate '.Core' versions of their libraries specifically targeting .NET Core.</span></span> <span data-ttu-id="f33fd-174">Пакеты корпоративной библиотеки являются примером публикации сообщества ". NetCore" альтернативы.</span><span class="sxs-lookup"><span data-stu-id="f33fd-174">Enterprise Library packages are an example of the community publishing ".NetCore" alternatives.</span></span> <span data-ttu-id="f33fd-175">В других случаях для .NET Standard доступны новые SDK для конкретной службы (иногда с разными названиями пакетов).</span><span class="sxs-lookup"><span data-stu-id="f33fd-175">In other cases, newer SDKs for a particular service (sometimes with different package names) are available for .NET Standard.</span></span> <span data-ttu-id="f33fd-176">Если альтернативы нет, вы можете продолжить использование пакетов, ориентированных на рамочную программу .NET, имея в виду, что вам нужно будет тщательно протестировать их после запуска на .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-176">If no alternatives are available, you can proceed using the .NET Framework-targeted packages, bearing in mind that you'll need to test them thoroughly once running on .NET Core.</span></span>

<span data-ttu-id="f33fd-177">Образец Bean Trader имеет следующие верхние уровни зависимостей NuGet:</span><span class="sxs-lookup"><span data-stu-id="f33fd-177">The Bean Trader sample has the following top-level NuGet dependencies:</span></span>

- [<span data-ttu-id="f33fd-178">**Castle.Windsor, версия 4.1.1**</span><span class="sxs-lookup"><span data-stu-id="f33fd-178">**Castle.Windsor, version 4.1.1**</span></span>](https://www.castleproject.org/projects/windsor/)  

  <span data-ttu-id="f33fd-179">Этот пакет нацелен на .NET Standard 1.6, поэтому он работает на ядро .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-179">This package targets .NET Standard 1.6, so it works on .NET Core.</span></span>

- [<span data-ttu-id="f33fd-180">**Microsoft.CodeAnalysis.FxCopAnalyzers, версия 2.6.3**</span><span class="sxs-lookup"><span data-stu-id="f33fd-180">**Microsoft.CodeAnalysis.FxCopAnalyzers, version 2.6.3**</span></span>](https://www.nuget.org/packages/Microsoft.CodeAnalysis.FxCopAnalyzers/2.6.3)  
  <span data-ttu-id="f33fd-181">Это мета-пакет, так что не сразу очевидно, какие платформы он поддерживает, но [документация](https://github.com/dotnet/roslyn-analyzers#microsoftcodeanalysisfxcopanalyzers) указывает, что его новейшая версия (2.9.2) будет работать как для .NET Framework и .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-181">This is a meta-package, so it's not immediately obvious which platforms it supports, but [documentation](https://github.com/dotnet/roslyn-analyzers#microsoftcodeanalysisfxcopanalyzers) indicates that its newest version (2.9.2) will work for both .NET Framework and .NET Core.</span></span>

- [<span data-ttu-id="f33fd-182">**Nito.AsyncEx, версия 4.0.1**</span><span class="sxs-lookup"><span data-stu-id="f33fd-182">**Nito.AsyncEx, version 4.0.1**</span></span>](https://www.nuget.org/packages/Nito.AsyncEx/4.0.1)  

  <span data-ttu-id="f33fd-183">Этот пакет не нацелен на ядро .NET, но делает более новая версия 5.0.</span><span class="sxs-lookup"><span data-stu-id="f33fd-183">This package doesn't target .NET Core, but the newer 5.0 version does.</span></span> <span data-ttu-id="f33fd-184">Это часто происходит при миграции, поскольку многие пакеты NuGet недавно добавили поддержку .NET Standard, но старые версии проектов будут нацелены только на .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="f33fd-184">This is common when migrating because many NuGet packages have added .NET Standard support recently, but older project versions will only target .NET Framework.</span></span> <span data-ttu-id="f33fd-185">Если разница в версии является лишь незначительным отличием версии, часто легко перейти на более новую версию.</span><span class="sxs-lookup"><span data-stu-id="f33fd-185">If the version difference is only a minor version difference, it's often easy to upgrade to the newer version.</span></span> <span data-ttu-id="f33fd-186">Поскольку это основное изменение версии, необходимо быть осторожным обновлением, так как в пакете могут быть изменения.</span><span class="sxs-lookup"><span data-stu-id="f33fd-186">Because this is a major version change, you need to be cautious upgrading since there could be breaking changes in the package.</span></span> <span data-ttu-id="f33fd-187">Существует путь вперед, хотя, что хорошо.</span><span class="sxs-lookup"><span data-stu-id="f33fd-187">There is a path forward, though, which is good.</span></span>

- [<span data-ttu-id="f33fd-188">**MahApps.Metro, версия 1.6.5**</span><span class="sxs-lookup"><span data-stu-id="f33fd-188">**MahApps.Metro, version 1.6.5**</span></span>](https://www.nuget.org/packages/MahApps.Metro/1.6.5)  

  <span data-ttu-id="f33fd-189">Этот пакет также не нацелен на ядро .NET, но имеет более новую предварительную версию (2.0-альфа), которая делает.</span><span class="sxs-lookup"><span data-stu-id="f33fd-189">This package also doesn't target .NET Core, but has a newer pre-release (2.0-alpha) that does.</span></span> <span data-ttu-id="f33fd-190">Опять же, вы должны смотреть на нарушение изменений, но новый пакет обнадеживает.</span><span class="sxs-lookup"><span data-stu-id="f33fd-190">Again, you have to look out for breaking changes, but the newer package is encouraging.</span></span>

<span data-ttu-id="f33fd-191">NuGet-зависимости образца Bean Trader нацелены либо на .NET Standard/.NET Core, либо имеют более новые версии, которые делают, так что здесь вряд ли будут какие-либо проблемы с блокировкой.</span><span class="sxs-lookup"><span data-stu-id="f33fd-191">The Bean Trader sample's NuGet dependencies all either target .NET Standard/.NET Core or have newer versions that do, so there are unlikely to be any blocking issues here.</span></span>

### <a name="upgrade-nuget-packages"></a><span data-ttu-id="f33fd-192">Обновление пакетов NuGet</span><span class="sxs-lookup"><span data-stu-id="f33fd-192">Upgrade NuGet packages</span></span>

<span data-ttu-id="f33fd-193">Если это возможно, было бы хорошо обновить версии любых пакетов, которые нацелены только на .NET Core или .NET Standard с более поздними версиями на данный момент (с проектом, все еще ориентированным на .NET Framework), чтобы обнаружить и устранить любые изменения на ранних стадиях.</span><span class="sxs-lookup"><span data-stu-id="f33fd-193">If possible, it would be good to upgrade the versions of any packages that only target .NET Core or .NET Standard with more recent versions at this point (with the project still targeting .NET Framework) to discover and address any breaking changes early.</span></span>

<span data-ttu-id="f33fd-194">Если вы не хотите вносить какие-либо существенные изменения в существующую версию приложения .NET Framework, это может подождать, пока у вас не будет нового файла проекта, ориентированного на .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-194">If you would rather not make any material changes to the existing .NET Framework version of the app, this can wait until you have a new project file targeting .NET Core.</span></span> <span data-ttu-id="f33fd-195">Однако обновление пакетов NuGet до версий, совместимых с ядром .NET, заблаговременно упрощает процесс миграции после создания нового файла проекта и уменьшает количество различий между версиями приложения .NET и .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-195">However, upgrading the NuGet packages to .NET Core-compatible versions ahead of time makes the migration process even easier once you create the new project file and reduces the number of differences between the .NET Framework and .NET Core versions of the app.</span></span>

<span data-ttu-id="f33fd-196">С образцом Bean Trader, все необходимые обновления могут быть сделаны легко (с помощью Visual Studio в NuGet менеджер пакета) с одним исключением: обновление от **MahApps.Metro 1.6.5** до **2.0** показывает нарушение изменения, связанные с темой и акцент управления AIS.</span><span class="sxs-lookup"><span data-stu-id="f33fd-196">With the Bean Trader sample, all of the necessary upgrades can be made easily (using Visual Studio's NuGet package manager) with one exception: upgrading from **MahApps.Metro 1.6.5** to **2.0** reveals breaking changes related to theme and accent management APIs.</span></span>

<span data-ttu-id="f33fd-197">В идеале приложение будет обновляться, чтобы использовать более новую версию пакета (поскольку это, скорее всего, будет работать на .NET Core).</span><span class="sxs-lookup"><span data-stu-id="f33fd-197">Ideally, the app would be updated to use the newer version of the package (since that is more likely to work on .NET Core).</span></span> <span data-ttu-id="f33fd-198">Однако в некоторых случаях это может оказаться неосуществимым.</span><span class="sxs-lookup"><span data-stu-id="f33fd-198">In some cases, however, that may not be feasible.</span></span> <span data-ttu-id="f33fd-199">В этих случаях не обновляйте **MahApps.Metro,** потому что необходимые изменения нетривиальны, и этот учебник фокусируется на переходе в .NET Core 3, а не на **MahApps.Metro 2.**</span><span class="sxs-lookup"><span data-stu-id="f33fd-199">In these cases, don't upgrade **MahApps.Metro** because the necessary changes are non-trivial and this tutorial focuses on migrating to .NET Core 3, not to **MahApps.Metro 2.**</span></span> <span data-ttu-id="f33fd-200">Кроме того, это низкорисковый .NET Рамочный зависимость, потому что приложение Bean Trader только осуществляет небольшую часть **MahApps.Metro**.</span><span class="sxs-lookup"><span data-stu-id="f33fd-200">Also, this is a low-risk .NET Framework dependency because the Bean Trader app only exercises a small part of **MahApps.Metro**.</span></span> <span data-ttu-id="f33fd-201">Это, конечно, потребует тестирования, чтобы убедиться, что все работает, как только миграция будет завершена.</span><span class="sxs-lookup"><span data-stu-id="f33fd-201">It will, of course, require testing to make sure everything's working once the migration is complete.</span></span> <span data-ttu-id="f33fd-202">Если бы это был реальный сценарий, было бы хорошо, чтобы подать вопрос для отслеживания работы, чтобы перейти к **Версии MahApps.Metro** 2.0, так как не делать миграции в настоящее время оставляет позади некоторые технические задолженности.</span><span class="sxs-lookup"><span data-stu-id="f33fd-202">If this were a real-world scenario, it would be good to file an issue to track the work to move to **MahApps.Metro** version 2.0 since not doing the migration now leaves behind some technical debt.</span></span>

<span data-ttu-id="f33fd-203">После обновления пакетов NuGet до последних версий группа `<PackageReference>` элементов в файле проекта образца Bean Trader должна выглядеть следующим образом.</span><span class="sxs-lookup"><span data-stu-id="f33fd-203">Once the NuGet packages are updated to recent versions, the `<PackageReference>` item group in the Bean Trader sample's project file should look like this.</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Castle.Windsor">
    <Version>4.1.1</Version>
  </PackageReference>
  <PackageReference Include="MahApps.Metro">
    <Version>1.6.5</Version>
  </PackageReference>
  <PackageReference Include="Microsoft.CodeAnalysis.FxCopAnalyzers">
    <Version>2.9.2</Version>
  </PackageReference>
  <PackageReference Include="Nito.AsyncEx">
    <Version>5.0.0</Version>
  </PackageReference>
</ItemGroup>
```

### <a name="net-framework-portability-analysis"></a><span data-ttu-id="f33fd-204">Анализ переносимости рамочной платформы .NET</span><span class="sxs-lookup"><span data-stu-id="f33fd-204">.NET Framework portability analysis</span></span>

<span data-ttu-id="f33fd-205">Как только вы поймете состояние зависимостей NuGet вашего проекта, следующее, что следует учитывать, это зависимости API-приложения для рамочного анима .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-205">Once you understand the state of your project's NuGet dependencies, the next thing to consider is .NET Framework API dependencies.</span></span> <span data-ttu-id="f33fd-206">Инструмент [анализа переносимости .NET](../../standard/analyzers/portability-analyzer.md) полезен для понимания того, какие из APIs .NET, которые используется вашим проектом, доступны на других платформах .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-206">The [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) tool is useful for understanding which of the .NET APIs your project uses are available on other .NET platforms.</span></span>

<span data-ttu-id="f33fd-207">Инструмент поставляется как [визуальный плагин Studio](https://marketplace.visualstudio.com/items?itemName=ConnieYau.NETPortabilityAnalyzer), [командный инструмент,](https://github.com/Microsoft/dotnet-apiport/releases)или завернутый в [простой графический интерфейс,](https://github.com/Microsoft/dotnet-apiport-ui)который упрощает его варианты.</span><span class="sxs-lookup"><span data-stu-id="f33fd-207">The tool comes as a [Visual Studio plugin](https://marketplace.visualstudio.com/items?itemName=ConnieYau.NETPortabilityAnalyzer), a [command-line tool](https://github.com/Microsoft/dotnet-apiport/releases), or wrapped in a [simple GUI](https://github.com/Microsoft/dotnet-apiport-ui), which simplifies its options.</span></span> <span data-ttu-id="f33fd-208">Вы можете прочитать больше об использовании анализа переносимости .NET (API Port) с помощью графического интерфейса в [настольных приложениях Porting для](https://devblogs.microsoft.com/dotnet/porting-desktop-apps-to-net-core/) записи в блоге .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-208">You can read more about using the .NET Portability Analyzer (API Port) using the GUI in the [Porting desktop apps to .NET Core](https://devblogs.microsoft.com/dotnet/porting-desktop-apps-to-net-core/) blog post.</span></span> <span data-ttu-id="f33fd-209">Если вы предпочитаете использовать командную строку, необходимые шаги:</span><span class="sxs-lookup"><span data-stu-id="f33fd-209">If you prefer to use the command line, the necessary steps are:</span></span>

1. <span data-ttu-id="f33fd-210">Загрузите [анализируемость портативности .NET,](https://github.com/Microsoft/dotnet-apiport/releases) если у вас его еще нет.</span><span class="sxs-lookup"><span data-stu-id="f33fd-210">Download the [.NET Portability Analyzer](https://github.com/Microsoft/dotnet-apiport/releases) if you don't already have it.</span></span>
1. <span data-ttu-id="f33fd-211">Убедитесь, что приложение .NET Framework, чтобы быть портировано, успешно портируетсборки (это хорошая идея до миграции независимо от того).</span><span class="sxs-lookup"><span data-stu-id="f33fd-211">Make sure the .NET Framework app to be ported builds successfully (this is a good idea prior to migration regardless).</span></span>
1. <span data-ttu-id="f33fd-212">Выполнить API Port с командной строкой, как это.</span><span class="sxs-lookup"><span data-stu-id="f33fd-212">Run API Port with a command line like this.</span></span>

    ```console
    ApiPort.exe analyze -f <PathToBeanTraderBinaries> -r html -r excel -t ".NET Core"
    ```

    <span data-ttu-id="f33fd-213">Аргумент `-f` определяет путь, содержащий файлы для анализа.</span><span class="sxs-lookup"><span data-stu-id="f33fd-213">The `-f` argument specifies the path containing the binaries to analyze.</span></span> <span data-ttu-id="f33fd-214">Аргумент `-r` определяет, какой формат вывода файла выхотитесь.</span><span class="sxs-lookup"><span data-stu-id="f33fd-214">The `-r` argument specifies which output file format you want.</span></span> <span data-ttu-id="f33fd-215">Аргумент `-t` определяет, какая платформа .NET для анализа использования API против.</span><span class="sxs-lookup"><span data-stu-id="f33fd-215">The `-t` argument specifies which .NET platform to analyze API usage against.</span></span> <span data-ttu-id="f33fd-216">В этом случае требуется ядро .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-216">In this case, you want .NET Core.</span></span>

<span data-ttu-id="f33fd-217">При открытии отчета HTML в первом разделе будут указаны все проанализированные файлы и какой процент APIs .NET они доступны на целевой платформе.</span><span class="sxs-lookup"><span data-stu-id="f33fd-217">When you open the HTML report, the first section will list all of the analyzed binaries and what percentage of the .NET APIs they use are available on the targeted platform.</span></span> <span data-ttu-id="f33fd-218">Процент сам по себе не имеет смысла.</span><span class="sxs-lookup"><span data-stu-id="f33fd-218">The percentage is not meaningful by itself.</span></span> <span data-ttu-id="f33fd-219">Что более полезно, так это увидеть конкретные AIS, которые отсутствуют.</span><span class="sxs-lookup"><span data-stu-id="f33fd-219">What's more useful is to see the specific APIs that are missing.</span></span> <span data-ttu-id="f33fd-220">Для этого выберите имя сборки или прокрутите вниз к отчетам для отдельных сборок.</span><span class="sxs-lookup"><span data-stu-id="f33fd-220">To do that, either select an assembly name or scroll down to the reports for individual assemblies.</span></span>

<span data-ttu-id="f33fd-221">Сосредоточьтесь на сборках, для получения исходного кода.</span><span class="sxs-lookup"><span data-stu-id="f33fd-221">Focus on the assemblies that you own the source code for.</span></span> <span data-ttu-id="f33fd-222">Например, в отчете Bean Trader ApiPort есть много бинарных файлов, но большинство из них принадлежат пакетам NuGet.</span><span class="sxs-lookup"><span data-stu-id="f33fd-222">In the Bean Trader ApiPort report, for example, there are many binaries listed, but most of them belong to NuGet packages.</span></span> <span data-ttu-id="f33fd-223">`Castle.Windsor`показывает, что это зависит от некоторых AБИИ System.Web, которые отсутствуют в .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-223">`Castle.Windsor` shows that it depends on some System.Web APIs that are missing in .NET Core.</span></span> <span data-ttu-id="f33fd-224">Это не вызывает беспокойства, потому что вы ранее проверили, что `Castle.Windsor` поддерживает .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-224">This isn't a concern because you previously verified that `Castle.Windsor` supports .NET Core.</span></span> <span data-ttu-id="f33fd-225">Часто пакеты NuGet имеют различные файлы для использования с различными платформами .NET, так что ли версия .NET Framework `Castle.Windsor` использует AAP System.Web или нет, не имеет значения до тех пор, пока пакет также нацелен на .NET Standard или .NET Core (что он и делает).</span><span class="sxs-lookup"><span data-stu-id="f33fd-225">It is common for NuGet packages to have different binaries for use with different .NET platforms, so whether the .NET Framework version of `Castle.Windsor` uses System.Web APIs or not is irrelevant as long as the package also targets .NET Standard or .NET Core (which it does).</span></span>

<span data-ttu-id="f33fd-226">С образцом Bean Trader, только двоичный, что вам нужно рассмотреть это **BeanTraderClient** `System.ServiceModel.ClientBase<T>.Close` и `System.ServiceModel.ClientBase<T>.Open`доклад показывает, что только два .NET AIS отсутствуют: и .</span><span class="sxs-lookup"><span data-stu-id="f33fd-226">With the Bean Trader sample, the only binary that you need to consider is **BeanTraderClient** and the report shows that only two .NET APIs are missing: `System.ServiceModel.ClientBase<T>.Close` and `System.ServiceModel.ClientBase<T>.Open`.</span></span>

![Отчет о переносимости BeanTraderClient](./media/convert-project-from-net-framework/portability-report.png)

<span data-ttu-id="f33fd-228">Они вряд ли будут блокированием вопросов, потому что AIS клиента WCF (в основном) поддерживаются на .NET Core, поэтому должны быть альтернативы, доступные для этих центральных AI.</span><span class="sxs-lookup"><span data-stu-id="f33fd-228">These are unlikely to be blocking issues because WCF Client APIs are (mostly) supported on .NET Core, so there must be alternatives available for these central APIs.</span></span> <span data-ttu-id="f33fd-229">В самом деле, глядя на `System.ServiceModel`'s <https://apisof.net>.NET Core поверхности (с помощью), вы видите, что Есть async альтернативы в .NET Core вместо.</span><span class="sxs-lookup"><span data-stu-id="f33fd-229">In fact, looking at `System.ServiceModel`'s .NET Core surface area (using <https://apisof.net>), you see that there are async alternatives in .NET Core instead.</span></span>

<span data-ttu-id="f33fd-230">Основываясь на этом отчете и предыдущем анализе зависимости NuGet, похоже, что не должно быть никаких серьезных проблем, перенося образец Bean Trader в ядро .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-230">Based on this report and the previous NuGet dependency analysis, it looks like there should be no major issues migrating the Bean Trader sample to .NET Core.</span></span> <span data-ttu-id="f33fd-231">Вы готовы к следующему шагу, в котором вы фактически начнете миграцию.</span><span class="sxs-lookup"><span data-stu-id="f33fd-231">You're ready for the next step in which you'll actually start the migration.</span></span>

## <a name="migrating-the-project-file"></a><span data-ttu-id="f33fd-232">Перенос файл проекта</span><span class="sxs-lookup"><span data-stu-id="f33fd-232">Migrating the project file</span></span>

<span data-ttu-id="f33fd-233">Если приложение не использует новый [формат файла проекта в стиле SDK,](../../core/tools/csproj.md)вам понадобится новый файл проекта для целевой цели .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-233">If your app isn't using the new [SDK-style project file format](../../core/tools/csproj.md), you'll need a new project file to target .NET Core.</span></span> <span data-ttu-id="f33fd-234">Можно заменить существующий файл csproj или, если вы предпочитаете сохранить существующий проект нетронутым в текущем состоянии, можно добавить новый файл csproj, ориентированный на .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-234">You can replace the existing csproj file or, if you prefer to keep the existing project untouched in its current state, you can add a new csproj file targeting .NET Core.</span></span> <span data-ttu-id="f33fd-235">Версии приложения для .NET Framework и .NET Core можно с помощью одного файла проекта `<TargetFrameworks>` в стиле SDK с [несколькими таргетингами](../../standard/library-guidance/cross-platform-targeting.md) (с указанием нескольких целей).</span><span class="sxs-lookup"><span data-stu-id="f33fd-235">You can build versions of the app for .NET Framework and .NET Core with a single SDK-style project file with [multi-targeting](../../standard/library-guidance/cross-platform-targeting.md) (specifying multiple `<TargetFrameworks>` targets).</span></span>

<span data-ttu-id="f33fd-236">Для создания нового файла проекта можно создать новый проект WPF в Visual Studio или использовать `dotnet new wpf` команду во временном каталоге для создания файла проекта, а затем скопировать/переименовать его в нужное место.</span><span class="sxs-lookup"><span data-stu-id="f33fd-236">To create the new project file, you can create a new WPF project in Visual Studio or use the `dotnet new wpf` command in a temporary directory to generate the project file and then copy/rename it to the correct location.</span></span> <span data-ttu-id="f33fd-237">Существует также созданный сообществом [инструмент, CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017), который может автоматизировать некоторые из миграции файла проекта.</span><span class="sxs-lookup"><span data-stu-id="f33fd-237">There is also a community-created tool, [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017), that can automate some of the project file migration.</span></span> <span data-ttu-id="f33fd-238">Инструмент полезен, но все еще нуждается в человеке, чтобы просмотреть результаты, чтобы убедиться, что все детали миграции верны.</span><span class="sxs-lookup"><span data-stu-id="f33fd-238">The tool is helpful but still needs a human to review the results to make sure all the details of the migration are correct.</span></span> <span data-ttu-id="f33fd-239">Одной из конкретных областей, с которыми инструмент не обрабатывает оптимально, является миграция пакетов NuGet из файлов *packages.config.*</span><span class="sxs-lookup"><span data-stu-id="f33fd-239">One particular area that the tool doesn't handle optimally is migrating NuGet packages from *packages.config* files.</span></span> <span data-ttu-id="f33fd-240">Если инструмент работает на файле проекта, который по-прежнему использует файл *packages.config* для ссылки на пакеты NuGet, он автоматически перекочегнет в `<PackageReference>` элементы, но добавит `<PackageReference>` элементы для *всех* пакетов, а не только для пакетов верхнего уровня.</span><span class="sxs-lookup"><span data-stu-id="f33fd-240">If the tool runs on a project file that still uses a *packages.config* file to reference NuGet packages, it will migrate to `<PackageReference>` elements automatically, but will add `<PackageReference>` elements for *all* of the packages instead of just top-level ones.</span></span> <span data-ttu-id="f33fd-241">Если вы уже перешли`<PackageReference>` на элементы Visual Studio (как вы сделали в этом примере), то инструмент может помочь с остальной частью преобразования.</span><span class="sxs-lookup"><span data-stu-id="f33fd-241">If you have already migrated to`<PackageReference>` elements with Visual Studio (as you've done in this sample), then the tool can help with the rest of the conversion.</span></span> <span data-ttu-id="f33fd-242">Как Скотт Hanselman рекомендует в [своем блоге на мигрирующих файлов csproj](https://www.hanselman.com/blog/UpgradingAnExistingNETProjectFilesToTheLeanNewCSPROJFormatFromNETCore.aspx), перенос вручную является образовательным и даст лучшие результаты, если у вас есть только несколько проектов в порт.</span><span class="sxs-lookup"><span data-stu-id="f33fd-242">Like Scott Hanselman recommends in [his blog post on migrating csproj files](https://www.hanselman.com/blog/UpgradingAnExistingNETProjectFilesToTheLeanNewCSPROJFormatFromNETCore.aspx), porting by hand is educational and will give better results if you only have a few projects to port.</span></span> <span data-ttu-id="f33fd-243">Но если вы портируете десятки или сотни файлов проектов, то инструмент, как «CsprojToVs2017» может быть помощью.</span><span class="sxs-lookup"><span data-stu-id="f33fd-243">But if you are porting dozens or hundreds of project files, then a tool like [CsprojToVs2017] can be a help.</span></span>

<span data-ttu-id="f33fd-244">Чтобы `dotnet new wpf` создать новый файл проекта для образца Bean Trader, запустите во временном каталоге и переместите генерируемый файл *.csproj* в папку *BeanTraderClient* и переименуйте его **BeanTraderClient.Core.csproj**.</span><span class="sxs-lookup"><span data-stu-id="f33fd-244">To create a new project file for the Bean Trader sample, run `dotnet new wpf` in a temporary directory and move the generated *.csproj* file into the *BeanTraderClient* folder and rename it **BeanTraderClient.Core.csproj**.</span></span>

<span data-ttu-id="f33fd-245">Поскольку новый формат файла проекта автоматически включает файлы C', *файлы resx* и файлы XAML, которые он находит в каталоге или под его каталогом, файл проекта уже почти завершен!</span><span class="sxs-lookup"><span data-stu-id="f33fd-245">Because the new project file format automatically includes C# files, *resx* files, and XAML files that it finds in or under its directory, the project file is already almost complete!</span></span> <span data-ttu-id="f33fd-246">Чтобы завершить миграцию, откройте старые и новые файлы проекта бок о бок и просмотрите старый, чтобы увидеть, нужно ли мигрировать какую-либо содержащуюся в нем информацию.</span><span class="sxs-lookup"><span data-stu-id="f33fd-246">To finish the migration, open the old and new project files side-by-side and look through the old one to see if any information it contains needs to be migrated.</span></span> <span data-ttu-id="f33fd-247">В примере Bean Trader следует скопировать в новый проект следующие элементы:</span><span class="sxs-lookup"><span data-stu-id="f33fd-247">In the Bean Trader sample case, the following items should be copied to the new project:</span></span>

- <span data-ttu-id="f33fd-248">Все `<RootNamespace>` `<AssemblyName>`свойства `<ApplicationIcon>` и свойства должны быть скопированы.</span><span class="sxs-lookup"><span data-stu-id="f33fd-248">The `<RootNamespace>`, `<AssemblyName>`, and `<ApplicationIcon>` properties should all be copied.</span></span>

- <span data-ttu-id="f33fd-249">Также необходимо добавить `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>` свойство в новый файл проекта, так как образец `[AssemblyTitle]`Bean Trader включает атрибуты уровня сборки (например) в AssemblyInfo.cs файле.</span><span class="sxs-lookup"><span data-stu-id="f33fd-249">You also need to add a `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>` property to the new project file since the Bean Trader sample includes assembly-level attributes (like `[AssemblyTitle]`) in an AssemblyInfo.cs file.</span></span> <span data-ttu-id="f33fd-250">По умолчанию новые проекты в стиле SDK будут автоматически генерировать эти атрибуты на основе свойств в файле csproj.</span><span class="sxs-lookup"><span data-stu-id="f33fd-250">By default, new SDK-style projects will autogenerate these attributes based on properties in the csproj file.</span></span> <span data-ttu-id="f33fd-251">Поскольку вы не хотите, чтобы это произошло в этом случае (автогенерируемые атрибуты будут конфликтовать с атрибутами из AssemblyInfo.cs), вы отрабатываете аутогенерируемые атрибуты с `<GenerateAssemblyInfo>`помощью .</span><span class="sxs-lookup"><span data-stu-id="f33fd-251">Because you don't want that to happen in this case (the autogenerated attributes would conflict with those from AssemblyInfo.cs), you disable the autogenerated attributes with `<GenerateAssemblyInfo>`.</span></span>

- <span data-ttu-id="f33fd-252">Хотя *файлы resx* автоматически включаются `<Resource>` в качестве встроенных ресурсов, другие элементы, такие как изображения, не являются.</span><span class="sxs-lookup"><span data-stu-id="f33fd-252">Although *resx* files are automatically included as embedded resources, other `<Resource>` items like images are not.</span></span> <span data-ttu-id="f33fd-253">Таким образом, `<Resource>` скопируйте элементы для встраивания файлов изображений и значков.</span><span class="sxs-lookup"><span data-stu-id="f33fd-253">So, copy the `<Resource>` elements for embedding image and icon files.</span></span> <span data-ttu-id="f33fd-254">Вы можете упростить ссылки png на одну строку, используя поддержку нового формата файла проекта для шаблонов globbing: `<Resource Include="**\*.png" />`.</span><span class="sxs-lookup"><span data-stu-id="f33fd-254">You can simplify the png references to a single line by using the new project file format's support for globbing patterns: `<Resource Include="**\*.png" />`.</span></span>

- <span data-ttu-id="f33fd-255">Аналогичным `<None>` образом элементы включаются автоматически, но по умолчанию они не скопированы в каталог вывода.</span><span class="sxs-lookup"><span data-stu-id="f33fd-255">Similarly, `<None>` items are included automatically, but they aren't copied to the output directory, by default.</span></span> <span data-ttu-id="f33fd-256">Поскольку проект Bean `<None>` Trader включает элемент, *который* скопирован `PreserveNewest` в каталог вывода (с использованием поведения), необходимо обновить автоматически населенный `<None>` элемент для этого файла, как это.</span><span class="sxs-lookup"><span data-stu-id="f33fd-256">Because the Bean Trader project includes a `<None>` item that *is* copied to the output directory (using `PreserveNewest` behaviors), you need to update the automatically populated `<None>` item for that file, like this.</span></span>

  ```xml
  <None Update="BeanTrader.pfx">
    <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </None>
  ```

- <span data-ttu-id="f33fd-257">Образец Bean Trader включает файл XAML (Default.Accent.xaml) `Content` `Page`как (а не как), потому что темы и акценты, определенные в этом файле, загружаются из XAML файла во время выполнения, а не встраиваются в само приложение.</span><span class="sxs-lookup"><span data-stu-id="f33fd-257">The Bean Trader sample includes a XAML file (Default.Accent.xaml) as `Content` (rather than as a `Page`) because themes and accents defined in this file are loaded from the file's XAML at runtime, rather than being embedded in the app itself.</span></span> <span data-ttu-id="f33fd-258">Новая система проекта автоматически включает `<Page>`этот файл как, однако, так как это файл XAML.</span><span class="sxs-lookup"><span data-stu-id="f33fd-258">The new project system automatically includes this file as a `<Page>`, however, since it's a XAML file.</span></span> <span data-ttu-id="f33fd-259">Таким образом, вам нужно как удалить файл XAML в качестве страницы ()`<Page Remove="**\Default.Accent.xaml" />`и добавить его в качестве содержания.</span><span class="sxs-lookup"><span data-stu-id="f33fd-259">So, you need to both remove the XAML file as a page (`<Page Remove="**\Default.Accent.xaml" />`) and add it as content.</span></span>

  ```xml
  <Content Include="Resources\Themes\Default.Accent.xaml">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </Content>
  ```

- <span data-ttu-id="f33fd-260">Наконец, добавьте ссылки NuGet, скопируя `<ItemGroup>` со всеми элементами. `<PackageReference>`</span><span class="sxs-lookup"><span data-stu-id="f33fd-260">Finally, add NuGet references by copying the `<ItemGroup>` with all the `<PackageReference>` elements.</span></span> <span data-ttu-id="f33fd-261">Если ранее вы не обновляли пакеты NuGet до версий, совместимых с ядром .NET, вы можете сделать это сейчас, когда ссылки на пакет находятся в проекте .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-261">If you hadn't previously upgraded the NuGet packages to .NET Core-compatible versions, you could do that now that the package references are in a .NET Core-specific project.</span></span>

<span data-ttu-id="f33fd-262">На этом этапе можно добавить новый проект в решение BeanTrader и открыть его в Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="f33fd-262">At this point, it should be possible to add the new project to the BeanTrader solution and open it in Visual Studio.</span></span> <span data-ttu-id="f33fd-263">Проект должен выглядеть правильно в `dotnet restore BeanTraderClient.Core.csproj` Solution **Explorer**и должен успешно восстановить пакеты (с двумя ожидаемыми предупреждениями, связанными с версией MahApps.Metro, которая используется для таргетинга .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="f33fd-263">The project should look correct in **Solution Explorer**, and `dotnet restore BeanTraderClient.Core.csproj` should successfully restore packages (with two expected warnings related to the MahApps.Metro version you're using targeting .NET Framework).</span></span>

<span data-ttu-id="f33fd-264">Хотя оба файла проекта можно сохранить бок о бок (и даже может быть желательно, если вы хотите продолжать строить старый проект точно так же, как это было), это усложняет процесс миграции (два проекта будут пытаться использовать те же папки ядра и объята) и, как правило, не является необходимым.</span><span class="sxs-lookup"><span data-stu-id="f33fd-264">Although it's possible to keep both project files side-by-side (and may even be desirable if you want to keep building the old project exactly as it was), it complicates the migration process (the two projects will try to use the same bin and obj folders) and usually isn't necessary.</span></span> <span data-ttu-id="f33fd-265">Если вы хотите построить для целей .NET Core и `<TargetFramework>netcoreapp3.0</TargetFramework>` .NET Framework, `<TargetFrameworks>netcoreapp3.0;net472</TargetFrameworks>` вы можете заменить свойство в новом файле проекта вместо этого.</span><span class="sxs-lookup"><span data-stu-id="f33fd-265">If you want to build for both .NET Core and .NET Framework targets, you can replace the `<TargetFramework>netcoreapp3.0</TargetFramework>` property in the new project file with `<TargetFrameworks>netcoreapp3.0;net472</TargetFrameworks>` instead.</span></span> <span data-ttu-id="f33fd-266">Для примера Bean Trader удалите старый файл проекта (BeanTraderClient.csproj), так как он больше не нужен.</span><span class="sxs-lookup"><span data-stu-id="f33fd-266">For the Bean Trader sample, delete the old project file (BeanTraderClient.csproj) since it's no longer needed.</span></span> <span data-ttu-id="f33fd-267">Если вы предпочитаете хранить оба файла проекта, не забудьте построить их на различные выходные и промежуточные пути вывода.</span><span class="sxs-lookup"><span data-stu-id="f33fd-267">If you prefer to keep both project files, be sure to have them build to different output and intermediate output paths.</span></span>

## <a name="fix-build-issues"></a><span data-ttu-id="f33fd-268">Исправление проблем со сборкой</span><span class="sxs-lookup"><span data-stu-id="f33fd-268">Fix build issues</span></span>

<span data-ttu-id="f33fd-269">Третьим этапом процесса переноса является создание проекта.</span><span class="sxs-lookup"><span data-stu-id="f33fd-269">The third step of the porting process is getting the project to build.</span></span> <span data-ttu-id="f33fd-270">Некоторые приложения уже успешно строятся после того, как файл проекта будет преобразован в проект в стиле SDK.</span><span class="sxs-lookup"><span data-stu-id="f33fd-270">Some apps will already build successfully once the project file is converted to an SDK-style project.</span></span> <span data-ttu-id="f33fd-271">Если это так для вашего приложения, поздравления!</span><span class="sxs-lookup"><span data-stu-id="f33fd-271">If that's the case for your app, congratulations!</span></span> <span data-ttu-id="f33fd-272">Вы можете перейти к шагу 4.</span><span class="sxs-lookup"><span data-stu-id="f33fd-272">You can go on to Step 4.</span></span> <span data-ttu-id="f33fd-273">Другим приложениям потребуются некоторые обновления, чтобы получить их здание для .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-273">Other apps will need some updates to get them building for .NET Core.</span></span> <span data-ttu-id="f33fd-274">Если вы попытаетесь запустить `dotnet build` на bean Trader образец проекта сейчас, например, (или построить его в Visual Studio), там будет много ошибок, но вы получите их быстро.</span><span class="sxs-lookup"><span data-stu-id="f33fd-274">If you try to run `dotnet build` on the Bean Trader sample project now, for example, (or build it in Visual Studio), there will be many errors, but you'll get them fixed quickly.</span></span>

### <a name="systemservicemodel-references-and-microsoftwindowscompatibility"></a><span data-ttu-id="f33fd-275">Ссылки На System.ServiceModel и Microsoft.Windows.Совместимость</span><span class="sxs-lookup"><span data-stu-id="f33fd-275">System.ServiceModel references and Microsoft.Windows.Compatibility</span></span>

<span data-ttu-id="f33fd-276">Распространенным источником ошибок являются ссылки на AA, которые доступны для .NET Core, но не автоматически включены в метапакет приложения .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-276">A common source of errors is missing references for APIs that are available for .NET Core but not automatically included in the .NET Core app metapackage.</span></span> <span data-ttu-id="f33fd-277">Чтобы решить эту проблему, `Microsoft.Windows.Compatibility` следует ссылку на пакет.</span><span class="sxs-lookup"><span data-stu-id="f33fd-277">To address this, you should reference the `Microsoft.Windows.Compatibility` package.</span></span> <span data-ttu-id="f33fd-278">Пакет совместимости включает в себя широкий набор AI, которые распространены в настольных приложениях Windows, таких как клиент WCF, службы каталога, реестр, конфигурация, AI ACLs и многое другое.</span><span class="sxs-lookup"><span data-stu-id="f33fd-278">The compatibility package includes a broad set of APIs that are common in Windows desktop apps, such as WCF client, directory services, registry, configuration, ACLs APIs, and more.</span></span>

<span data-ttu-id="f33fd-279">С образцом Bean Trader большинство ошибок сборки <xref:System.ServiceModel> связано с отсутствующими типами.</span><span class="sxs-lookup"><span data-stu-id="f33fd-279">With the Bean Trader sample, the majority of the build errors are due to missing <xref:System.ServiceModel> types.</span></span> <span data-ttu-id="f33fd-280">Эти проблемы могут быть решены путем ссылки на необходимые пакеты WCF NuGet.</span><span class="sxs-lookup"><span data-stu-id="f33fd-280">These could be addressed by referencing the necessary WCF NuGet packages.</span></span> <span data-ttu-id="f33fd-281">WCF клиент AIS являются одними из тех, кто присутствует в `Microsoft.Windows.Compatibility` пакете, хотя, так ссылаясь на пакет совместимости является еще лучшим решением (поскольку он также решает любые вопросы, связанные с AI, а также решения wCF вопросов, что пакет совместимости делает доступными).</span><span class="sxs-lookup"><span data-stu-id="f33fd-281">WCF client APIs are among those present in the `Microsoft.Windows.Compatibility` package, though, so referencing the compatibility package is an even better solution (since it also addresses any issues related to APIs as well as solutions to the WCF issues that the compatibility package makes available).</span></span> <span data-ttu-id="f33fd-282">Пакет `Microsoft.Windows.Compatibility` помогает в большинстве сценариев переноса портаации .NET Core 3.0 WPF и WinForms.</span><span class="sxs-lookup"><span data-stu-id="f33fd-282">The `Microsoft.Windows.Compatibility` package helps in most .NET Core 3.0 WPF and WinForms porting scenarios.</span></span> <span data-ttu-id="f33fd-283">После добавления ссылки NuGet на `Microsoft.Windows.Compatibility`, только одна ошибка сборки остается!</span><span class="sxs-lookup"><span data-stu-id="f33fd-283">After adding the NuGet reference to `Microsoft.Windows.Compatibility`, only one build error remains!</span></span>

### <a name="cleaning-up-unused-files"></a><span data-ttu-id="f33fd-284">Очистка неиспользованных файлов</span><span class="sxs-lookup"><span data-stu-id="f33fd-284">Cleaning up unused files</span></span>

<span data-ttu-id="f33fd-285">Один из всплывает тип проблемы миграции, который возникает, часто относится к файлам C и XAML, которые ранее не были включены в сборку, подхватив новые проекты в стиле SDK, которые включают в себя *весь* исходный исход автоматически.</span><span class="sxs-lookup"><span data-stu-id="f33fd-285">One type of migration issue that comes up often relates to C# and XAML files that weren't previously included in the build getting picked up by the new SDK-style projects that include *all* source automatically.</span></span>

<span data-ttu-id="f33fd-286">Следующая ошибка сборки, которую вы видите в примере Bean Trader, относится к плохой реализации интерфейса в *OldUnusedViewModel.cs.*</span><span class="sxs-lookup"><span data-stu-id="f33fd-286">The next build error you see in the Bean Trader sample refers to a bad interface implementation in *OldUnusedViewModel.cs*.</span></span> <span data-ttu-id="f33fd-287">Имя файла является подсказкой, но при проверке вы обнаружите, что этот исходный файл неверен.</span><span class="sxs-lookup"><span data-stu-id="f33fd-287">The file name is a hint, but on inspection, you'll find that this source file is incorrect.</span></span> <span data-ttu-id="f33fd-288">Ранее проблемы не возникали, так как не были включены в первоначальный проект .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="f33fd-288">It didn't cause issues previously because it wasn't included in the original .NET Framework project.</span></span> <span data-ttu-id="f33fd-289">Исходные файлы, которые присутствовали на диске, но не включены в старый *csproj,* теперь автоматически включаются.</span><span class="sxs-lookup"><span data-stu-id="f33fd-289">Source files that were present on disk but not included in the old *csproj* are included automatically now.</span></span>

<span data-ttu-id="f33fd-290">Для одноразовых проблем, как это, это легко сравнить с предыдущим *csproj,* чтобы подтвердить, `<Compile Remove="" />` что файл не нужен, а затем либо его или, если исходный файл не нужен в любом месте больше, удалить его.</span><span class="sxs-lookup"><span data-stu-id="f33fd-290">For one-off issues like this, it's easy to compare to the previous *csproj* to confirm that the file isn't needed, and then either `<Compile Remove="" />` it or, if the source file isn't needed anywhere anymore, delete it.</span></span> <span data-ttu-id="f33fd-291">В этом случае можно просто удалить *OldUnusedViewModel.cs.*</span><span class="sxs-lookup"><span data-stu-id="f33fd-291">In this case, it's safe to just delete *OldUnusedViewModel.cs*.</span></span>

<span data-ttu-id="f33fd-292">Если у вас есть много исходных файлов, которые должны быть исключены таким образом, `<EnableDefaultCompileItems>` вы можете отключить автоматическое включение файлов C, установив свойство на ложное в файле проекта.</span><span class="sxs-lookup"><span data-stu-id="f33fd-292">If you have many source files that would need to be excluded this way, you can disable auto-inclusion of C# files by setting the `<EnableDefaultCompileItems>` property to false in the project file.</span></span> <span data-ttu-id="f33fd-293">Затем можно скопировать `<Compile Include>` элементы из старого файла проекта в новый, чтобы построить только источники, которые вы намеревались включить.</span><span class="sxs-lookup"><span data-stu-id="f33fd-293">Then, you can copy `<Compile Include>` items from the old project file to the new one in order to only build sources you intended to include.</span></span> <span data-ttu-id="f33fd-294">Аналогичным `<EnableDefaultPageItems>` образом, может быть использован для выключения `<EnableDefaultItems>` автоматического включения страниц XAML и может управлять как с одним свойством.</span><span class="sxs-lookup"><span data-stu-id="f33fd-294">Similarly, `<EnableDefaultPageItems>` can be used to turn off auto-inclusion of XAML pages and `<EnableDefaultItems>` can control both with a single property.</span></span>

### <a name="a-brief-aside-on-multi-pass-compilers"></a><span data-ttu-id="f33fd-295">Краткое в сторону на многопроходных компиляторов</span><span class="sxs-lookup"><span data-stu-id="f33fd-295">A brief aside on multi-pass compilers</span></span>

<span data-ttu-id="f33fd-296">После удаления файла-нарушителя из образца Bean Trader, вы можете перестроить и получите четыре ошибки.</span><span class="sxs-lookup"><span data-stu-id="f33fd-296">After removing the offending file from the Bean Trader sample, you can re-build and will get four errors.</span></span> <span data-ttu-id="f33fd-297">Разве у тебя его раньше не было?</span><span class="sxs-lookup"><span data-stu-id="f33fd-297">Didn't you have one before?</span></span> <span data-ttu-id="f33fd-298">Почему выросло количество ошибок?</span><span class="sxs-lookup"><span data-stu-id="f33fd-298">Why did the number of errors go up?</span></span> <span data-ttu-id="f33fd-299">Компилятор C' является [многопроходным компилятором.](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes)</span><span class="sxs-lookup"><span data-stu-id="f33fd-299">The C# compiler is a [multi-pass compiler](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes).</span></span> <span data-ttu-id="f33fd-300">Это означает, что он проходит через каждый исходный файл дважды.</span><span class="sxs-lookup"><span data-stu-id="f33fd-300">This means that it goes through each source file twice.</span></span> <span data-ttu-id="f33fd-301">Во-первых, компилятор просто просматривает метаданные и декларации в каждом исходном файле и определяет любые проблемы уровня декларации.</span><span class="sxs-lookup"><span data-stu-id="f33fd-301">First, the compiler just looks at metadata and declarations in each source file and identifies any declaration-level problems.</span></span> <span data-ttu-id="f33fd-302">Таковы ошибки, которые вы исправили.</span><span class="sxs-lookup"><span data-stu-id="f33fd-302">Those are the errors you've fixed.</span></span> <span data-ttu-id="f33fd-303">Затем он снова проходит через код, чтобы построить источник C-кода в IL; это второй набор ошибок, которые вы видите сейчас.</span><span class="sxs-lookup"><span data-stu-id="f33fd-303">Then it goes through the code again to build the C# source into IL; those are the second set of errors that you're seeing now.</span></span>

> [!NOTE]
> <span data-ttu-id="f33fd-304">Компилятор СИ делает [больше, чем просто два прохода,](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes)но конечный результат заключается в том, что ошибки компилятора для больших изменений кода, как это, как правило, приходят в две волны.</span><span class="sxs-lookup"><span data-stu-id="f33fd-304">The C# compiler does [more than just two passes](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes), but the end result is that compiler errors for large code changes like this tend to come in two waves.</span></span>

### <a name="third-party-dependency-fixes-castlewindsor"></a><span data-ttu-id="f33fd-305">Исправления зависимости сторонних сторон (Castle.Windsor)</span><span class="sxs-lookup"><span data-stu-id="f33fd-305">Third-party dependency fixes (Castle.Windsor)</span></span>

<span data-ttu-id="f33fd-306">Другой класс проблем, который возникает в некоторых сценариях миграции, — это различия API между версиями зависимостей .NET и .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-306">Another class of issue that comes up in some migration scenarios is API differences between .NET Framework and .NET Core versions of dependencies.</span></span> <span data-ttu-id="f33fd-307">Даже если пакет NuGet нацелен как на .NET Framework, так и на стандарт .NET или .NET Core, могут быть разные библиотеки для использования с различными целями .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-307">Even if a NuGet package targets both .NET Framework and .NET Standard or .NET Core, there may be different libraries for use with different .NET targets.</span></span> <span data-ttu-id="f33fd-308">Это позволяет пакетам поддерживать множество различных платформ .NET, что может потребовать различных реализаций.</span><span class="sxs-lookup"><span data-stu-id="f33fd-308">This allows the packages to support many different .NET platforms, which may require different implementations.</span></span> <span data-ttu-id="f33fd-309">Это также означает, что при таргетинге на различные платформы .NET могут быть небольшие различия в API в библиотеках.</span><span class="sxs-lookup"><span data-stu-id="f33fd-309">It also means that there may be small API differences in the libraries when targeting different .NET platforms.</span></span>

<span data-ttu-id="f33fd-310">Следующий набор ошибок, которые вы увидите в `Castle.Windsor` примере Bean Trader, связан с AIS.</span><span class="sxs-lookup"><span data-stu-id="f33fd-310">The next set of errors you'll see in the Bean Trader sample are related to `Castle.Windsor` APIs.</span></span> <span data-ttu-id="f33fd-311">Проект .NET Core Bean Trader использует `Castle.Windsor` ту же версию, что и проект.NET Framework (4.1.1), но реализация для этих двух платформ несколько отличается.</span><span class="sxs-lookup"><span data-stu-id="f33fd-311">The .NET Core Bean Trader project uses the same version of `Castle.Windsor` as the .NET Framework-targeted project (4.1.1), but the implementations for those two platforms are slightly different.</span></span>

<span data-ttu-id="f33fd-312">В этом случае вы видите следующие проблемы, которые необходимо устранить:</span><span class="sxs-lookup"><span data-stu-id="f33fd-312">In this case, you see the following issues that need to be fixed:</span></span>

1. <span data-ttu-id="f33fd-313">`Castle.MicroKernel.Registration.Classes.FromThisAssembly`недоступен на основе .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-313">`Castle.MicroKernel.Registration.Classes.FromThisAssembly` isn't available on .NET Core.</span></span> <span data-ttu-id="f33fd-314">Существует, однако, аналогичный `Classes.FromAssemblyContaining` API доступны, так `Classes.FromThisAssembly()` что мы `Classes.FromAssemblyContaining(t)`можем `t` заменить оба использования с вызовами, где тип делает вызов.</span><span class="sxs-lookup"><span data-stu-id="f33fd-314">There is, however, the similar API `Classes.FromAssemblyContaining` available, so we can replace both uses of `Classes.FromThisAssembly()` with calls to `Classes.FromAssemblyContaining(t)`, where `t` is the type making the call.</span></span>
1. <span data-ttu-id="f33fd-315">Аналогичным образом, `Castle.Windsor.Installer.FromAssembly`в *Bootstrapper.cs*, . Это недоступно на ядро .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-315">Similarly, in *Bootstrapper.cs*, `Castle.Windsor.Installer.FromAssembly`.This is unavailable on .NET Core.</span></span> <span data-ttu-id="f33fd-316">Вместо этого этот вызов `FromAssembly.Containing(typeof(Bootstrapper))`может быть заменен .</span><span class="sxs-lookup"><span data-stu-id="f33fd-316">Instead, that call can be replaced with `FromAssembly.Containing(typeof(Bootstrapper))`.</span></span>

### <a name="updating-wcf-client-usage"></a><span data-ttu-id="f33fd-317">Обновление использования клиентов WCF</span><span class="sxs-lookup"><span data-stu-id="f33fd-317">Updating WCF client usage</span></span>

<span data-ttu-id="f33fd-318">`Castle.Windsor` Зафиксировав различия, последняя оставшаяся ошибка сборки в `BeanTraderServiceClient` проекте .NET `DuplexClientBase`Core Bean `Open` Trader заключается в том, что (который вытекает из) не имеет метода.</span><span class="sxs-lookup"><span data-stu-id="f33fd-318">Having fixed the `Castle.Windsor` differences, the last remaining build error in the .NET Core Bean Trader project is that `BeanTraderServiceClient` (which derives from `DuplexClientBase`) doesn't have an `Open` method.</span></span> <span data-ttu-id="f33fd-319">Это неудивительно, так как это API, который был выделен анализатором переносимости .NET в начале этого процесса миграции.</span><span class="sxs-lookup"><span data-stu-id="f33fd-319">This isn't surprising since this is an API that was highlighted by the .NET Portability Analyzer at the beginning of this migration process.</span></span> <span data-ttu-id="f33fd-320">Глядя `BeanTraderServiceClient` на обращает наше внимание на более широкую проблему, однако.</span><span class="sxs-lookup"><span data-stu-id="f33fd-320">Looking at `BeanTraderServiceClient` draws our attention to a larger issue, though.</span></span> <span data-ttu-id="f33fd-321">Этот клиент WCF был аутогенерирован инструментом [Svcutil.exe.](../../framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md)</span><span class="sxs-lookup"><span data-stu-id="f33fd-321">This WCF client was autogenerated by the [Svcutil.exe](../../framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md) tool.</span></span>

<span data-ttu-id="f33fd-322">**Клиенты WCF, генерируемые Svcutil, предназначены для использования на .NET Framework.**</span><span class="sxs-lookup"><span data-stu-id="f33fd-322">**WCF clients generated by Svcutil are meant for use on .NET Framework.**</span></span>

<span data-ttu-id="f33fd-323">Решения, которые используют svcutil генерируемых wCF клиентов необходимо будет регенерировать .NET стандартных совместимых клиентов для использования с .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-323">Solutions that use svcutil-generated WCF clients will need to regenerate .NET Standard-compatible clients for use with .NET Core.</span></span> <span data-ttu-id="f33fd-324">Одна из основных причин, по которой старые клиенты не будут работать, заключается в том, что они зависят от конфигурации приложения для определения привязок и конечных точек WCF.</span><span class="sxs-lookup"><span data-stu-id="f33fd-324">One of the main reasons the old clients won't work is that they depend on app configuration for defining WCF bindings and endpoints.</span></span> <span data-ttu-id="f33fd-325">Поскольку AI.net Standard WCF могут работать кросс-платформенными (где AIS System.Configuration недоступны), клиенты WCF для сценариев .NET Core и .NET Standard должны определять привязки и конечные точки за программно, а не в конфигурации.</span><span class="sxs-lookup"><span data-stu-id="f33fd-325">Because .NET Standard WCF APIs can work cross-platform (where System.Configuration APIs aren't available), WCF clients for .NET Core and .NET Standard scenarios must define bindings and endpoints programmatically instead of in configuration.</span></span>

<span data-ttu-id="f33fd-326">В самом деле, любое использование `<system.serviceModel>` клиента WCF, который зависит от раздела app.config (будь то созданный с Svcutil или вручную) должны быть изменены для работы на .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-326">In fact, any WCF client usage that depends on the `<system.serviceModel>` app.config section (whether created with Svcutil or manually) will need to be changed to work on .NET Core.</span></span>

<span data-ttu-id="f33fd-327">Существует два способа автоматического генерации клиентов WCF со стандартной со вместимостью .NET:</span><span class="sxs-lookup"><span data-stu-id="f33fd-327">There are two ways to automatically generate .NET Standard-compatible WCF clients:</span></span>

- <span data-ttu-id="f33fd-328">Инструмент `dotnet-svcutil` является инструментом .NET, который генерирует клиентов WCF таким образом, что похож на то, как Svcutil работал ранее.</span><span class="sxs-lookup"><span data-stu-id="f33fd-328">The `dotnet-svcutil` tool is a .NET tool that generates WCF clients in a way that is similar to how Svcutil worked previously.</span></span>
- <span data-ttu-id="f33fd-329">Visual Studio может генерировать клиентов WCF, используя опцию [ссылки на веб-службы WCF](../../core/additional-tools/wcf-web-service-reference-guide.md) из своей функции Connected Services.</span><span class="sxs-lookup"><span data-stu-id="f33fd-329">Visual Studio can generate WCF clients using the [WCF Web Service Reference](../../core/additional-tools/wcf-web-service-reference-guide.md) option of its Connected Services feature.</span></span>

<span data-ttu-id="f33fd-330">Любой подход работает хорошо.</span><span class="sxs-lookup"><span data-stu-id="f33fd-330">Either approach works well.</span></span> <span data-ttu-id="f33fd-331">Кроме того, конечно, вы можете написать код клиента WCF самостоятельно.</span><span class="sxs-lookup"><span data-stu-id="f33fd-331">Alternatively, of course, you could write the WCF client code yourself.</span></span> <span data-ttu-id="f33fd-332">Для этого примера я решил использовать функцию службы Visual Studio Connected Service.</span><span class="sxs-lookup"><span data-stu-id="f33fd-332">For this sample, I chose to use the Visual Studio Connected Service feature.</span></span> <span data-ttu-id="f33fd-333">Для этого нажмите правой кнопкой мыши на *проекте BeanTraderClient.Core* в explorer решения Visual Studio и выберите **Добавить** > **подключенную службу.**</span><span class="sxs-lookup"><span data-stu-id="f33fd-333">To do that, right-click on the *BeanTraderClient.Core* project in Visual Studio's solution explorer and select **Add** > **Connected Service**.</span></span> <span data-ttu-id="f33fd-334">Затем выберите поставщика справочных услуг Web-сервиса WCF.</span><span class="sxs-lookup"><span data-stu-id="f33fd-334">Next, choose the WCF Web Service Reference Provider.</span></span> <span data-ttu-id="f33fd-335">Это позволит привести к диалогу, где вы можете указать адрес`localhost:8080` бэкэнда Bean Trader веб-службы (если вы работаете на сервере локально) и пространство имен, которые генерируются типы должны использовать (**BeanTrader.Service**, например).</span><span class="sxs-lookup"><span data-stu-id="f33fd-335">This will bring up a dialog where you can specify the address of the backend Bean Trader web service (`localhost:8080` if you are running the server locally) and the namespace that generated types should use (**BeanTrader.Service**, for example).</span></span>

![WCF Веб-служба Справочная Служба Диалог](./media/convert-project-from-net-framework/connected-service-dialog.png)

<span data-ttu-id="f33fd-337">После выбора кнопки **«Завершение»** в проект добавляется новый узла Connected Services и под этим узла Reference.csм, содержащим новый клиент .NET Standard WCF для доступа к службе Bean Trader.</span><span class="sxs-lookup"><span data-stu-id="f33fd-337">After you select the **Finish** button, a new Connected Services node is added to the project and a Reference.cs file is added under that node containing the new .NET Standard WCF client for accessing the Bean Trader service.</span></span> <span data-ttu-id="f33fd-338">Если вы `GetEndpointAddress` посмотрите `GetBindingForEndpoint` на или методы в этом файле, вы увидите, что привязки и конечные точки теперь генерируются программно (вместо того, чтобы с помощью конфигурации приложения).</span><span class="sxs-lookup"><span data-stu-id="f33fd-338">If you look at the `GetEndpointAddress` or `GetBindingForEndpoint` methods in that file, you'll see that bindings and endpoints are now generated programmatically (instead of via app config).</span></span> <span data-ttu-id="f33fd-339">Функция «Добавить подключенные службы» может также добавить ссылки на некоторые пакеты System.ServiceModel в файле проекта, которые не нужны, так как все необходимые пакеты WCF включены через Microsoft.Windows.Compatibility.</span><span class="sxs-lookup"><span data-stu-id="f33fd-339">The 'Add Connected Services' feature may also add references to some System.ServiceModel packages in the project file, which aren't needed since all necessary WCF packages are included via Microsoft.Windows.Compatibility.</span></span> <span data-ttu-id="f33fd-340">Проверьте csproj, чтобы увидеть, если `<PackageReference>` какие-либо дополнительные элементы System.ServiceModel были добавлены, и если да, удалить их.</span><span class="sxs-lookup"><span data-stu-id="f33fd-340">Check the csproj to see if any extra System.ServiceModel `<PackageReference>` items have been added, and if so, remove them.</span></span>

<span data-ttu-id="f33fd-341">Наш проект имеет новые классы клиентов WCF в настоящее время (в *Reference.cs),* но он также по-прежнему имеет старые (в BeanTrader.cs).</span><span class="sxs-lookup"><span data-stu-id="f33fd-341">Our project has new WCF client classes now (in *Reference.cs*), but it also still has the old ones (in BeanTrader.cs).</span></span> <span data-ttu-id="f33fd-342">На данный момент существует два варианта:</span><span class="sxs-lookup"><span data-stu-id="f33fd-342">There are two options at this point:</span></span>

- <span data-ttu-id="f33fd-343">Если вы хотите иметь возможность построить исходный проект .NET Framework (наряду с новым `<Compile Remove="BeanTrader.cs" />` целевым проектом .NET Core), вы можете использовать элемент в csproj-файле проекта .NET Core, чтобы в версиях приложения .NET Framework и .NET Core использовались различные клиенты WCF.</span><span class="sxs-lookup"><span data-stu-id="f33fd-343">If you want to be able to build the original .NET Framework project (alongside the new .NET Core-targeted one), you can use a `<Compile Remove="BeanTrader.cs" />` item in the .NET Core project's csproj file so that the .NET Framework and .NET Core versions of the app use different WCF clients.</span></span> <span data-ttu-id="f33fd-344">Преимущество этого заключается в том, что существующий проект .NET Framework останется неизменным, но имеет недостаток, что код с использованием генерируемых клиентов WCF `#if` может быть немного иным в случае .NET Core, чем в проекте .NET Framework, так что вам, вероятно, придется использовать директивы для условного компиляции некоторых пользователей WCF (создание клиентов, например), чтобы работать в одну сторону при построении для .NET Core и другим способом, когда построен для .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-344">This has the advantage of leaving the existing .NET Framework project unchanged, but has the disadvantage that code using the generated WCF clients may need to be slightly different in the .NET Core case than it was in the .NET Framework project, so you'll likely need to use `#if` directives to conditionally compile some WCF client usage (creating clients, for example) to work one way when built for .NET Core and another way when built for .NET Framework.</span></span>

- <span data-ttu-id="f33fd-345">Если, с другой стороны, неприемлемы некоторые оттоки кода в существующем проекте .NET Framework, можно удалить *BeanTrader.cs* все вместе.</span><span class="sxs-lookup"><span data-stu-id="f33fd-345">If, on the other hand, some code churn in the existing .NET Framework project is acceptable, you can remove *BeanTrader.cs* all together.</span></span> <span data-ttu-id="f33fd-346">Поскольку новый клиент WCF построен для .NET Standard, он будет работать как в сценариях .NET Core, так и в рамочных .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-346">Because the new WCF client is built for .NET Standard, it will work in both .NET Core and .NET Framework scenarios.</span></span> <span data-ttu-id="f33fd-347">Если вы строите для .NET Framework в дополнение к .NET Core (либо с помощью мультитаргетинга или с двумя файлами csproj), вы можете использовать этот новый *файл Reference.cs* для обеих целей.</span><span class="sxs-lookup"><span data-stu-id="f33fd-347">If you are building for .NET Framework in addition to .NET Core (either by multi-targeting or by having two csproj files), you can use this new *Reference.cs* file for both targets.</span></span> <span data-ttu-id="f33fd-348">Этот подход имеет то преимущество, что код не нужно будет раздвоить для поддержки двух различных клиентов WCF; один и тот же код будет использоваться везде.</span><span class="sxs-lookup"><span data-stu-id="f33fd-348">This approach has the advantage that the code won't need to bifurcate to support two different WCF clients; the same code will be used everywhere.</span></span> <span data-ttu-id="f33fd-349">Недостатком является то, что он включает в себя изменение (предположительно стабильный) рамочный проект .NET.</span><span class="sxs-lookup"><span data-stu-id="f33fd-349">The drawback is that it involves changing the (presumably stable) .NET Framework project.</span></span>

<span data-ttu-id="f33fd-350">В случае с образцом Bean Trader можно внести небольшие изменения в исходный проект, если он упрощает миграцию, поэтому выполните следующие действия, чтобы согласовать использование клиентов WCF:</span><span class="sxs-lookup"><span data-stu-id="f33fd-350">In the case of the Bean Trader sample, you can make small changes to the original project if it makes migration easier, so follow these steps to reconcile WCF client usage:</span></span>

01. <span data-ttu-id="f33fd-351">Добавьте новый Reference.cs файл в проект .NET *Framework BeanTraderClient.csproj,* используя контекстное меню «Добавить существующий элемент» от исследователя решения.</span><span class="sxs-lookup"><span data-stu-id="f33fd-351">Add the new Reference.cs file to the .NET Framework *BeanTraderClient.csproj* project using the 'Add existing item' context menu from the solution explorer.</span></span> <span data-ttu-id="f33fd-352">Не забудьте добавить "как ссылка", так что тот же файл используется в обоих проектах (в отличие от копирования файла C').</span><span class="sxs-lookup"><span data-stu-id="f33fd-352">Be sure to add 'as link' so that the same file is used by both projects (as opposed to copying the C# file).</span></span> <span data-ttu-id="f33fd-353">Если вы строите как для .NET Core, так и для .NET Framework с одним csproj (с использованием мультитаргетинга), то этот шаг не требуется.</span><span class="sxs-lookup"><span data-stu-id="f33fd-353">If you are building for both .NET Core and .NET Framework with a single csproj (using multi-targeting) then this step isn't necessary.</span></span>

01. <span data-ttu-id="f33fd-354">Удалить *BeanTrader.cs*.</span><span class="sxs-lookup"><span data-stu-id="f33fd-354">Delete *BeanTrader.cs*.</span></span>

01. <span data-ttu-id="f33fd-355">Новый клиент WCF похож на старый, но ряд именных пространств в генерируемом коде различны.</span><span class="sxs-lookup"><span data-stu-id="f33fd-355">The new WCF client is similar to the old one, but a number of namespaces in the generated code are different.</span></span> <span data-ttu-id="f33fd-356">Из-за этого необходимо обновить проект так, чтобы типы клиентов WCF использовались с BeanTrader.Service (или любого имени, которое вы выбрали) вместо BeanTrader.Model или без пространства имен.</span><span class="sxs-lookup"><span data-stu-id="f33fd-356">Because of this, it is necessary to update the project so that WCF client types are used from BeanTrader.Service (or whatever namespace name you chose) instead of BeanTrader.Model or without a namespace.</span></span> <span data-ttu-id="f33fd-357">Строительство *BeanTraderClient.Core.csproj* поможет определить, где эти изменения должны быть сделаны.</span><span class="sxs-lookup"><span data-stu-id="f33fd-357">Building *BeanTraderClient.Core.csproj* will help to identify where these changes need to be made.</span></span> <span data-ttu-id="f33fd-358">Исправления будут необходимы как в C,, так и в исходных файлах XAML.</span><span class="sxs-lookup"><span data-stu-id="f33fd-358">Fixes will be needed both in C# and in XAML source files.</span></span>

01. <span data-ttu-id="f33fd-359">Наконец, вы обнаружите, что есть ошибка в *BeanTraderServiceClientFactory.cs,* `BeanTraderServiceClient` потому что доступные конструкторы для типа изменились.</span><span class="sxs-lookup"><span data-stu-id="f33fd-359">Finally, you'll discover that there is an error in *BeanTraderServiceClientFactory.cs* because the available constructors for the `BeanTraderServiceClient` type have changed.</span></span> <span data-ttu-id="f33fd-360">Раньше можно было поставить `InstanceContext` аргумент (который `CallbackHandler` был `Castle.Windsor` создан с помощью контейнера IoC).</span><span class="sxs-lookup"><span data-stu-id="f33fd-360">It used to be possible to supply an `InstanceContext` argument (which was created using a `CallbackHandler` from the `Castle.Windsor` IoC container).</span></span> <span data-ttu-id="f33fd-361">Новые конструкторы создают `CallbackHandler`новые s.</span><span class="sxs-lookup"><span data-stu-id="f33fd-361">The new constructors create new `CallbackHandler`s.</span></span> <span data-ttu-id="f33fd-362">Есть, однако, конструкторы `BeanTraderServiceClient`в 's базовый тип, которые соответствуют тому, что вы хотите.</span><span class="sxs-lookup"><span data-stu-id="f33fd-362">There are, however, constructors in `BeanTraderServiceClient`'s base type that match what you want.</span></span> <span data-ttu-id="f33fd-363">Поскольку автоматически генерируемый код клиента WCF существует в частичных классах, его можно легко расширить.</span><span class="sxs-lookup"><span data-stu-id="f33fd-363">Since the autogenerated WCF client code all exists in partial classes, you can easily extend it.</span></span> <span data-ttu-id="f33fd-364">Для этого создайте новый файл под названием *BeanTraderServiceClient.cs* а затем создайте частичный класс с тем же именем (с помощью пространства имен BeanTrader.Service).</span><span class="sxs-lookup"><span data-stu-id="f33fd-364">To do this, create a new file called *BeanTraderServiceClient.cs* and then create a partial class with that same name (using the BeanTrader.Service namespace).</span></span> <span data-ttu-id="f33fd-365">Затем добавьте один конструктор к частичному типу, как показано здесь.</span><span class="sxs-lookup"><span data-stu-id="f33fd-365">Then, add one constructor to the partial type as shown here.</span></span>

    ```csharp
    public BeanTraderServiceClient(System.ServiceModel.InstanceContext callbackInstance) :
        base(callbackInstance, EndpointConfiguration.NetTcpBinding_BeanTraderService)
            { }
    ```

<span data-ttu-id="f33fd-366">С этими изменениями, образец Bean Trader теперь будет использовать новый клиент WCF, совместимый `Open` со стандартом `await OpenAsync` .NET, и вы можете сделать окончательное решение об изменении вызова в *TradingService.cs* использовать вместо этого.</span><span class="sxs-lookup"><span data-stu-id="f33fd-366">With those changes made, the Bean Trader sample will now be using a new .NET Standard-compatible WCF client and you can make the final fix of changing the `Open` call in *TradingService.cs* to use `await OpenAsync` instead.</span></span>

<span data-ttu-id="f33fd-367">С WCF вопросы рассматриваются, .NET Основная версия образца Bean Trader теперь строит чисто!</span><span class="sxs-lookup"><span data-stu-id="f33fd-367">With the WCF issues addressed, the .NET Core version of the Bean Trader sample now builds cleanly!</span></span>

## <a name="runtime-testing"></a><span data-ttu-id="f33fd-368">Тестирование времени выполнения</span><span class="sxs-lookup"><span data-stu-id="f33fd-368">Runtime testing</span></span>

<span data-ttu-id="f33fd-369">Легко забыть, что миграционная работа не выполняется, как только проект строится чисто против .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f33fd-369">It's easy to forget that migration work isn't done as soon as the project builds cleanly against .NET Core.</span></span> <span data-ttu-id="f33fd-370">Важно также оставить время для тестирования портативного приложения.</span><span class="sxs-lookup"><span data-stu-id="f33fd-370">It's important to leave time for testing the ported app, too.</span></span> <span data-ttu-id="f33fd-371">Как только вещи успешно строятся, убедитесь, что приложение работает и работает как ожидалось, особенно если вы используете какие-либо пакеты, ориентированные на .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="f33fd-371">Once things build successfully, make sure the app runs and works as expected, especially if you are using any packages targeting .NET Framework.</span></span>

<span data-ttu-id="f33fd-372">Давайте попробуем запустить портированное приложение Bean Trader и посмотрим, что произойдет.</span><span class="sxs-lookup"><span data-stu-id="f33fd-372">Let's try launching the ported Bean Trader app and see what happens.</span></span> <span data-ttu-id="f33fd-373">Приложение не получает далеко, прежде чем сбой со следующим исключением.</span><span class="sxs-lookup"><span data-stu-id="f33fd-373">The app doesn't get far before failing with the following exception.</span></span>

```output
System.Configuration.ConfigurationErrorsException: 'Configuration system failed to initialize'

Inner Exception
ConfigurationErrorsException: Unrecognized configuration section system.serviceModel.
```

<span data-ttu-id="f33fd-374">Это имеет смысл, конечно.</span><span class="sxs-lookup"><span data-stu-id="f33fd-374">This makes sense, of course.</span></span> <span data-ttu-id="f33fd-375">Помните, что WCF больше не использует конфигурацию приложения, поэтому старый раздел system.serviceModel файла app.config должен быть удален.</span><span class="sxs-lookup"><span data-stu-id="f33fd-375">Remember that WCF no longer uses app configuration, so the old system.serviceModel section of the app.config file needs to be removed.</span></span> <span data-ttu-id="f33fd-376">Обновленный клиент WCF содержит всю ту же информацию в своем коде, поэтому раздел конфигурации больше не нужен.</span><span class="sxs-lookup"><span data-stu-id="f33fd-376">The updated WCF client includes all of the same information in its code, so the config section isn't needed anymore.</span></span> <span data-ttu-id="f33fd-377">Если вы хотите, чтобы конечная точка WCF была настраиваемой в app.config, вы можете добавить ее в качестве настройки приложения и обновить клиентский код WCF для извлечения конечную точку службы WCF из конфигурации.</span><span class="sxs-lookup"><span data-stu-id="f33fd-377">If you wanted the WCF endpoint to be configurable in app.config, you could add it as an app setting and update the WCF client code to retrieve the WCF service endpoint from configuration.</span></span>

<span data-ttu-id="f33fd-378">После удаления раздела system.serviceModel *app.config,* приложение запускает, но не с другим исключением, когда пользователь вписывается.</span><span class="sxs-lookup"><span data-stu-id="f33fd-378">After removing the system.serviceModel section of *app.config*, the app launches but fails with another exception when a user signs in.</span></span>

```output
System.PlatformNotSupportedException: 'Operation is not supported on this platform.'
```

<span data-ttu-id="f33fd-379">Неподдерживаемый API `Func<T>.BeginInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f33fd-379">The unsupported API is `Func<T>.BeginInvoke`.</span></span> <span data-ttu-id="f33fd-380">Как поясняется в [dotnet/corefx-5940](https://github.com/dotnet/corefx/issues/5940), .NET `BeginInvoke` `EndInvoke` Core не поддерживает и методы по типам делегатов из-за базовых зависимостей ремотирования.</span><span class="sxs-lookup"><span data-stu-id="f33fd-380">As explained in [dotnet/corefx#5940](https://github.com/dotnet/corefx/issues/5940), .NET Core doesn't support the `BeginInvoke` and `EndInvoke` methods on delegate types due to underlying remoting dependencies.</span></span> <span data-ttu-id="f33fd-381">Эта проблема и ее исправление объясняются более подробно в [Migrating Delegate.BeginInvoke призывает к .NET Core](https://devblogs.microsoft.com/dotnet/migrating-delegate-begininvoke-calls-for-net-core/) блоге, но суть в том, что `BeginInvoke` и `EndInvoke` звонки должны быть заменены `Task.Run` (или async альтернативы, если это возможно).</span><span class="sxs-lookup"><span data-stu-id="f33fd-381">This issue and its fix are explained in more detail in the [Migrating Delegate.BeginInvoke Calls for .NET Core](https://devblogs.microsoft.com/dotnet/migrating-delegate-begininvoke-calls-for-net-core/) blog post, but the gist is that `BeginInvoke` and `EndInvoke` calls should be replaced with `Task.Run` (or async alternatives, if possible).</span></span> <span data-ttu-id="f33fd-382">Применяя общее решение `BeginInvoke` здесь, вызов может `Invoke` быть `Task.Run`заменен вызовом, запущенным .</span><span class="sxs-lookup"><span data-stu-id="f33fd-382">Applying the general solution here, the `BeginInvoke` call can be replaced with an `Invoke` call launched by `Task.Run`.</span></span>

```csharp
Task.Run(() =>
{
    return userInfoRetriever.Invoke();
}).ContinueWith(result =>
{
    // BeginInvoke's callback is replaced with ContinueWith
    var task = result.ConfigureAwait(false);
    CurrentTrader = task.GetAwaiter().GetResult();
}, TaskScheduler.Default);
```

<span data-ttu-id="f33fd-383">После удаления `BeginInvoke` использования приложение Bean Trader успешно работает на .NET Core!</span><span class="sxs-lookup"><span data-stu-id="f33fd-383">After removing the `BeginInvoke` usage, the Bean Trader app runs successfully on .NET Core!</span></span>

![Трейдер по бемболу работает на ядро .NET](./media/convert-project-from-net-framework/running-on-core.png)

<span data-ttu-id="f33fd-385">Все приложения отличаются друг от друга, поэтому конкретные шаги, необходимые для переноса собственных приложений в .NET Core, будут варьироваться.</span><span class="sxs-lookup"><span data-stu-id="f33fd-385">All apps are different, so the specific steps needed to migrate your own apps to .NET Core will vary.</span></span> <span data-ttu-id="f33fd-386">Но, надеюсь, образец Bean Trader демонстрирует общий рабочий процесс и типы проблем, которые можно ожидать.</span><span class="sxs-lookup"><span data-stu-id="f33fd-386">But hopefully the Bean Trader sample demonstrates the general workflow and the types of issues that can be expected.</span></span> <span data-ttu-id="f33fd-387">И, несмотря на длину этой статьи, фактические изменения, необходимые в образце Bean Trader, чтобы заставить его работать на .NET Core были довольно ограничены.</span><span class="sxs-lookup"><span data-stu-id="f33fd-387">And, despite this article's length, the actual changes needed in the Bean Trader sample to make it work on .NET Core were fairly limited.</span></span> <span data-ttu-id="f33fd-388">Многие приложения мигрируют в ядро .NET таким же образом; с ограниченнымили или даже не требуется изменений кода.</span><span class="sxs-lookup"><span data-stu-id="f33fd-388">Many apps migrate to .NET Core in this same way; with limited or even no code changes needed.</span></span>
