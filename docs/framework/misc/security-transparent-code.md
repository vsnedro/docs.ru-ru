---
title: Прозрачный для системы безопасности код
description: Изучите назначение прозрачной модели кода, определение уровня прозрачности и применение прозрачности в целях безопасности.
ms.date: 03/30/2017
helpviewer_keywords:
- transparent code
- security-transparent code
ms.assetid: 4f3dd841-82f7-4659-aab0-6d2db2166c65
ms.openlocfilehash: a167efe12b88f796fba4abc6d60ebffe4693709a
ms.sourcegitcommit: 0fa2b7b658bf137e813a7f4d09589d64c148ebf5
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 07/14/2020
ms.locfileid: "86309850"
---
# <a name="security-transparent-code"></a><span data-ttu-id="15afe-103">Прозрачный для системы безопасности код</span><span class="sxs-lookup"><span data-stu-id="15afe-103">Security-Transparent Code</span></span>

[!INCLUDE[net_security_note](../../../includes/net-security-note-md.md)]

<span data-ttu-id="15afe-104">Система безопасности включает в себя три основных компонента, взаимодействующих друг с другом: песочницы, разрешения и компоненты обеспечения безопасности.</span><span class="sxs-lookup"><span data-stu-id="15afe-104">Security involves three interacting pieces: sandboxing, permissions, and enforcement.</span></span> <span data-ttu-id="15afe-105">Технология песочниц включает в себя процесс создания изолированных доменов, в которых определенный код рассматривается как код с полным доверием, а другой код ограничивается набором разрешений, предоставленных песочнице.</span><span class="sxs-lookup"><span data-stu-id="15afe-105">Sandboxing refers to the practice of creating isolated domains where some code is treated as fully trusted and other code is restricted to the permissions in the grant set for the sandbox.</span></span> <span data-ttu-id="15afe-106">Код приложения, выполняемый с набором прав песочницы, считается прозрачным (то есть неспособным выполнять действия, влияющие на безопасность).</span><span class="sxs-lookup"><span data-stu-id="15afe-106">The application code that runs within the grant set of the sandbox is considered to be transparent; that is, it cannot perform any operations that can affect security.</span></span> <span data-ttu-id="15afe-107">Набор прав, предоставленных песочнице, определяется свидетельством (класс <xref:System.Security.Policy.Evidence>).</span><span class="sxs-lookup"><span data-stu-id="15afe-107">The grant set for the sandbox is determined by evidence (<xref:System.Security.Policy.Evidence> class).</span></span> <span data-ttu-id="15afe-108">Свидетельство определяет разрешения, необходимые для песочниц, а также возможные типы создаваемых песочниц.</span><span class="sxs-lookup"><span data-stu-id="15afe-108">Evidence identifies what specific permissions are required by sandboxes, and what kinds of sandboxes can be created.</span></span> <span data-ttu-id="15afe-109">Технологии обеспечения безопасности позволяют прозрачному коду выполнять только те действия, которые соответствуют набору предоставленных прав.</span><span class="sxs-lookup"><span data-stu-id="15afe-109">Enforcement refers to allowing transparent code to execute only within its grant set.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="15afe-110">Политика безопасности была ключевым элементом в предыдущих версиях платформы .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="15afe-110">Security policy was a key element in previous versions of the .NET Framework.</span></span> <span data-ttu-id="15afe-111">Начиная с .NET Framework 4, политика безопасности устарела.</span><span class="sxs-lookup"><span data-stu-id="15afe-111">Starting with the .NET Framework 4, security policy is obsolete.</span></span> <span data-ttu-id="15afe-112">Устранение политики безопасности выполняется независимо от прозрачности безопасности.</span><span class="sxs-lookup"><span data-stu-id="15afe-112">The elimination of security policy is separate from security transparency.</span></span> <span data-ttu-id="15afe-113">Сведения о влиянии этого изменения см. в разделе [совместимость и миграция политик управления доступом для кода](code-access-security-policy-compatibility-and-migration.md).</span><span class="sxs-lookup"><span data-stu-id="15afe-113">For information about the effects of this change, see [Code Access Security Policy Compatibility and Migration](code-access-security-policy-compatibility-and-migration.md).</span></span>

## <a name="purpose-of-the-transparency-model"></a><span data-ttu-id="15afe-114">Назначение модели прозрачности</span><span class="sxs-lookup"><span data-stu-id="15afe-114">Purpose of the Transparency Model</span></span>

<span data-ttu-id="15afe-115">Прозрачность — это механизм обеспечения безопасности, который отделяет код, выполняемый в рамках приложения, от кода, выполняемого в рамках инфраструктуры.</span><span class="sxs-lookup"><span data-stu-id="15afe-115">Transparency is an enforcement mechanism that separates code that runs as part of the application from code that runs as part of the infrastructure.</span></span> <span data-ttu-id="15afe-116">Технология прозрачности разделяет код, которому разрешено выполнять привилегированные действия (критически важный код, например вызов машинного кода), и код, не имеющий такого разрешения (прозрачный код).</span><span class="sxs-lookup"><span data-stu-id="15afe-116">Transparency draws a barrier between code that can do privileged things (critical code), such as calling native code, and code that cannot (transparent code).</span></span> <span data-ttu-id="15afe-117">Прозрачный код может выполнять команды в рамках используемого набора разрешений, но не может выполнять и содержать критический код, а также быть образованным от него.</span><span class="sxs-lookup"><span data-stu-id="15afe-117">Transparent code can execute commands within the bounds of the permission set it is operating in, but cannot execute, derive from, or contain critical code.</span></span>

<span data-ttu-id="15afe-118">Основной целью функции прозрачности является обеспечение простого и эффективного механизма для изоляции различных групп кода на основе привилегий.</span><span class="sxs-lookup"><span data-stu-id="15afe-118">The primary goal of transparency enforcement is to provide a simple, effective mechanism for isolating different groups of code based on privilege.</span></span> <span data-ttu-id="15afe-119">В контексте модели песочницы такие группы привилегий имеют полное доверие (то есть не ограничены) или частичное доверие (то есть ограничены набором разрешений, предоставленных песочнице).</span><span class="sxs-lookup"><span data-stu-id="15afe-119">Within the context of the sandboxing model, these privilege groups are either fully trusted (that is, not restricted) or partially trusted (that is, restricted to the permission set granted to the sandbox).</span></span>

> [!IMPORTANT]
> <span data-ttu-id="15afe-120">Модель прозрачности служит не только для управления доступом для кода.</span><span class="sxs-lookup"><span data-stu-id="15afe-120">The transparency model transcends code access security.</span></span> <span data-ttu-id="15afe-121">Прозрачность обеспечивается JIT-компилятором и действует вне зависимости от набора прав, предоставленного сборке (включая полное доверие).</span><span class="sxs-lookup"><span data-stu-id="15afe-121">Transparency is enforced by the just-in-time compiler and remains in effect regardless of the grant set for an assembly, including full trust.</span></span>

<span data-ttu-id="15afe-122">Прозрачность появилась в .NET Framework версии 2.0 для упрощения модели безопасности и для облегчения создания и развертывания безопасных библиотек и приложений.</span><span class="sxs-lookup"><span data-stu-id="15afe-122">Transparency was introduced in the .NET Framework version 2.0 to simplify the security model, and to make it easier to write and deploy secure libraries and applications.</span></span> <span data-ttu-id="15afe-123">Прозрачный код также используется в Microsoft Silverlight для упрощения разработки приложений с частичным доверием.</span><span class="sxs-lookup"><span data-stu-id="15afe-123">Transparent code is also used in Microsoft Silverlight, to simplify the development of partially trusted applications.</span></span>

> [!NOTE]
> <span data-ttu-id="15afe-124">При разработке частично доверенного приложения следует помнить о требованиях безопасности для целевых узлов.</span><span class="sxs-lookup"><span data-stu-id="15afe-124">When you develop a partially trusted application, you have to be aware of the permission requirements for your target hosts.</span></span> <span data-ttu-id="15afe-125">Вы можете создать приложение, использующее ресурсы, не разрешенные некоторыми узлами.</span><span class="sxs-lookup"><span data-stu-id="15afe-125">You can develop an application that uses resources that are not allowed by some hosts.</span></span> <span data-ttu-id="15afe-126">Компиляция приложения пройдет без ошибок, но при его загрузке в размещенную среду возникнет ошибка.</span><span class="sxs-lookup"><span data-stu-id="15afe-126">This application will compile without error, but will fail when it is loaded into the hosted environment.</span></span> <span data-ttu-id="15afe-127">Если приложение создано с помощью Visual Studio, вы можете включить отладку в режиме частичного доверия или с ограниченным набором разрешений из среды разработки.</span><span class="sxs-lookup"><span data-stu-id="15afe-127">If you have developed your application using Visual Studio, you can enable debugging in partial trust or in a restricted permission set from the development environment.</span></span> <span data-ttu-id="15afe-128">Для получения дополнительной информации см. [How to: Debug a ClickOnce Application with Restricted Permissions](/visualstudio/deployment/how-to-debug-a-clickonce-application-with-restricted-permissions).</span><span class="sxs-lookup"><span data-stu-id="15afe-128">For more information, see [How to: Debug a ClickOnce Application with Restricted Permissions](/visualstudio/deployment/how-to-debug-a-clickonce-application-with-restricted-permissions).</span></span> <span data-ttu-id="15afe-129">Функция вычисления разрешений, предоставляемая для приложений ClickOnce, также доступна для любого приложения с частичным доверием.</span><span class="sxs-lookup"><span data-stu-id="15afe-129">The Calculate Permissions feature provided for ClickOnce applications is also available for any partially trusted application.</span></span>

## <a name="specifying-the-transparency-level"></a><span data-ttu-id="15afe-130">Указание уровня прозрачности</span><span class="sxs-lookup"><span data-stu-id="15afe-130">Specifying the Transparency Level</span></span>

<span data-ttu-id="15afe-131">Атрибут уровня сборки <xref:System.Security.SecurityRulesAttribute> явным образом выбирает правила <xref:System.Security.SecurityRuleSet>, которым будет следовать сборка.</span><span class="sxs-lookup"><span data-stu-id="15afe-131">The assembly-level <xref:System.Security.SecurityRulesAttribute> attribute explicitly selects the <xref:System.Security.SecurityRuleSet> rules that the assembly will follow.</span></span> <span data-ttu-id="15afe-132">Правила организованы в многоуровневую систему, где более высокий уровень обеспечивает более строгое применение правил безопасности.</span><span class="sxs-lookup"><span data-stu-id="15afe-132">The rules are organized under a numeric level system, where higher levels mean tighter enforcement of security rules.</span></span>

<span data-ttu-id="15afe-133">Возможны следующие уровни:</span><span class="sxs-lookup"><span data-stu-id="15afe-133">The levels are as follows:</span></span>

- <span data-ttu-id="15afe-134">Level 2 ( <xref:System.Security.SecurityRuleSet.Level2> ) — правила прозрачности .NET Framework 4.</span><span class="sxs-lookup"><span data-stu-id="15afe-134">Level 2 (<xref:System.Security.SecurityRuleSet.Level2>) – the .NET Framework 4 transparency rules.</span></span>

- <span data-ttu-id="15afe-135">уровень 1 (<xref:System.Security.SecurityRuleSet.Level1>) — правила прозрачности .NET Framework 2.0.</span><span class="sxs-lookup"><span data-stu-id="15afe-135">Level 1 (<xref:System.Security.SecurityRuleSet.Level1>) – the .NET Framework 2.0 transparency rules.</span></span>

<span data-ttu-id="15afe-136">Основным различием между двумя уровнями безопасности является то, что уровень 1 не применяет правила безопасности для вызовов извне и предназначен только для обеспечения совместимости.</span><span class="sxs-lookup"><span data-stu-id="15afe-136">The primary difference between the two transparency levels is that level 1 does not enforce transparency rules for calls from outside the assembly and is intended only for compatibility.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="15afe-137">Прозрачность уровня 1 следует указывать только для совместимости, то есть уровень 1 следует задавать только для кода, разработанного на платформе .NET Framework 3.5 или более ранней версии и использующего атрибут <xref:System.Security.AllowPartiallyTrustedCallersAttribute> или не использующего модель прозрачности.</span><span class="sxs-lookup"><span data-stu-id="15afe-137">You should specify level 1 transparency for compatibility only; that is, specify level 1 only for code that was developed with the .NET Framework 3.5 or earlier that uses the <xref:System.Security.AllowPartiallyTrustedCallersAttribute> attribute or does not use the transparency model.</span></span> <span data-ttu-id="15afe-138">Например, используйте прозрачность уровня 1 для сборок .NET Framework 2.0, которые допускают вызовы из частично доверенных вызывающих объектов (APTCA).</span><span class="sxs-lookup"><span data-stu-id="15afe-138">For example, use level 1 transparency for .NET Framework 2.0 assemblies that allow calls from partially trusted callers (APTCA).</span></span> <span data-ttu-id="15afe-139">Для кода, разработанного для .NET Framework 4, всегда используйте прозрачность уровня 2.</span><span class="sxs-lookup"><span data-stu-id="15afe-139">For code that is developed for the .NET Framework 4, always use level 2 transparency.</span></span>

### <a name="level-2-transparency"></a><span data-ttu-id="15afe-140">Прозрачность уровня 2</span><span class="sxs-lookup"><span data-stu-id="15afe-140">Level 2 Transparency</span></span>

<span data-ttu-id="15afe-141">Прозрачность уровня 2 появилась в .NET Framework 4.</span><span class="sxs-lookup"><span data-stu-id="15afe-141">Level 2 transparency was introduced in the .NET Framework 4.</span></span> <span data-ttu-id="15afe-142">Ключевыми элементами этой модели являются следующие компоненты: прозрачный код, надежный с точки зрения безопасности код и критический с точки зрения безопасности код.</span><span class="sxs-lookup"><span data-stu-id="15afe-142">The three tenets of this model are transparent code, security-safe-critical code, and security-critical code.</span></span>

- <span data-ttu-id="15afe-143">Прозрачный код (вне зависимости от предоставленных разрешений, в том числе код с полным доверием) может вызывать только другой прозрачный или надежный с точки зрения безопасности код.</span><span class="sxs-lookup"><span data-stu-id="15afe-143">Transparent code, regardless of the permissions it is granted (including full trust), can call only other transparent code or security-safe-critical code.</span></span> <span data-ttu-id="15afe-144">Если код имеет частичное доверие, то он может выполнять только действия, разрешенные набором разрешений домена.</span><span class="sxs-lookup"><span data-stu-id="15afe-144">If the code is partially trusted, it can only perform actions that are allowed by the domain’s permission set.</span></span> <span data-ttu-id="15afe-145">Прозрачный код не может выполнять следующие действия:</span><span class="sxs-lookup"><span data-stu-id="15afe-145">Transparent code cannot do the following:</span></span>

  - <span data-ttu-id="15afe-146">выполнять операцию <xref:System.Security.CodeAccessPermission.Assert%2A> или повышение привилегий;</span><span class="sxs-lookup"><span data-stu-id="15afe-146">Perform an <xref:System.Security.CodeAccessPermission.Assert%2A> operation or elevation of privilege.</span></span>

  - <span data-ttu-id="15afe-147">содержать небезопасный или непроверяемый код;</span><span class="sxs-lookup"><span data-stu-id="15afe-147">Contain unsafe or unverifiable code.</span></span>

  - <span data-ttu-id="15afe-148">напрямую вызывать критический код;</span><span class="sxs-lookup"><span data-stu-id="15afe-148">Directly call critical code.</span></span>

  - <span data-ttu-id="15afe-149">вызывать машинный код или код с атрибутом <xref:System.Security.SuppressUnmanagedCodeSecurityAttribute>;</span><span class="sxs-lookup"><span data-stu-id="15afe-149">Call native code or code that has the <xref:System.Security.SuppressUnmanagedCodeSecurityAttribute> attribute.</span></span>

  - <span data-ttu-id="15afe-150">вызывать член, защищенный с помощью <xref:System.Security.Permissions.SecurityAction.LinkDemand>;</span><span class="sxs-lookup"><span data-stu-id="15afe-150">Call a member that is protected by a <xref:System.Security.Permissions.SecurityAction.LinkDemand>.</span></span>

  - <span data-ttu-id="15afe-151">наследовать от критических типов.</span><span class="sxs-lookup"><span data-stu-id="15afe-151">Inherit from critical types.</span></span>

    <span data-ttu-id="15afe-152">Кроме того, прозрачные методы не могут переопределять критические виртуальные методы или реализовывать критические методы интерфейсов.</span><span class="sxs-lookup"><span data-stu-id="15afe-152">In addition, transparent methods cannot override critical virtual methods or implement critical interface methods.</span></span>

- <span data-ttu-id="15afe-153">Надежный с точки зрения безопасности код имеет полное доверие, но может вызываться прозрачным кодом.</span><span class="sxs-lookup"><span data-stu-id="15afe-153">Security-safe-critical code is fully trusted but is callable by transparent code.</span></span> <span data-ttu-id="15afe-154">Он предоставляет ограниченный объем кода с полным доверием.</span><span class="sxs-lookup"><span data-stu-id="15afe-154">It exposes a limited surface area of full-trust code.</span></span> <span data-ttu-id="15afe-155">В надежном с точки зрения безопасности коде выполняются проверки на правильность и безопасность.</span><span class="sxs-lookup"><span data-stu-id="15afe-155">Correctness and security verifications happen in safe-critical code.</span></span>

- <span data-ttu-id="15afe-156">Критический с точки зрения безопасности код может вызывать любой код, но не может вызываться из прозрачного кода.</span><span class="sxs-lookup"><span data-stu-id="15afe-156">Security-critical code can call any code and is fully trusted, but it cannot be called by transparent code.</span></span>

### <a name="level-1-transparency"></a><span data-ttu-id="15afe-157">Прозрачность уровня 1</span><span class="sxs-lookup"><span data-stu-id="15afe-157">Level 1 Transparency</span></span>

<span data-ttu-id="15afe-158">Модель прозрачности уровня 1 появилась в .NET Framework версии 2.0 и позволяла разработчикам сократить объем кода, требующего аудита безопасности.</span><span class="sxs-lookup"><span data-stu-id="15afe-158">The level 1 transparency model was introduced in the .NET Framework version 2.0 to enable developers to reduce the amount of code that is subject to a security audit.</span></span> <span data-ttu-id="15afe-159">Несмотря на то, что прозрачность уровня 1 стала общедоступной в версии 2.0, она в основном использовалась только в корпорации Майкрософт для аудита безопасности.</span><span class="sxs-lookup"><span data-stu-id="15afe-159">Although level 1 transparency was publicly available in version 2.0, it was primarily used only within Microsoft for security auditing purposes.</span></span> <span data-ttu-id="15afe-160">Заметки позволяют разработчикам объявлять типы и члены, которые могут повышать уровень разрешений и выполнять другие доверенные (критические с точки зрения безопасности) действия, а также типы и члены, которые не могут выполнять такие действия (прозрачные с точки зрения безопасности).</span><span class="sxs-lookup"><span data-stu-id="15afe-160">Through annotations, developers are able to declare which types and members can perform security elevations and other trusted actions (security-critical) and which cannot (security-transparent).</span></span> <span data-ttu-id="15afe-161">Код, определенный как прозрачный, не требует подробного аудита безопасности.</span><span class="sxs-lookup"><span data-stu-id="15afe-161">Code that is identified as transparent does not require a high degree of security auditing.</span></span> <span data-ttu-id="15afe-162">Прозрачность уровня 1 ограничивает применение прозрачности пределами сборки.</span><span class="sxs-lookup"><span data-stu-id="15afe-162">Level 1 transparency states that the transparency enforcement is limited to within the assembly.</span></span> <span data-ttu-id="15afe-163">Другими словами, все открытые типы или члены, которые считаются критическими с точки зрения безопасности, являются таковыми только в пределах сборки.</span><span class="sxs-lookup"><span data-stu-id="15afe-163">In other words, any public types or members that are identified as security-critical are security-critical only within the assembly.</span></span> <span data-ttu-id="15afe-164">Чтобы обеспечить безопасность для этих типов и членов, когда они вызываются извне, необходимо использовать запросы компоновки с полным доверием.</span><span class="sxs-lookup"><span data-stu-id="15afe-164">If you want to enforce security for those types and members when they are called from outside the assembly, you must use link demands for full trust.</span></span> <span data-ttu-id="15afe-165">В противном случае открытые видимые критические типы и члены рассматриваются как надежные с точки зрения безопасности и могут вызываться частично доверенным кодом вне сборки.</span><span class="sxs-lookup"><span data-stu-id="15afe-165">If you do not, publicly visible security-critical types and members are treated as security-safe-critical and can be called by partially trusted code outside the assembly.</span></span>

<span data-ttu-id="15afe-166">Модель прозрачности уровня 1 имеет перечисленные ниже ограничения.</span><span class="sxs-lookup"><span data-stu-id="15afe-166">The level 1 transparency model has the following limitations:</span></span>

- <span data-ttu-id="15afe-167">Открытые критические с точки зрения безопасности типы и члены доступны из прозрачного с точки зрения безопасности кода.</span><span class="sxs-lookup"><span data-stu-id="15afe-167">Security-critical types and members that are public are accessible from security-transparent code.</span></span>

- <span data-ttu-id="15afe-168">Заметки прозрачности применяются только внутри сборки.</span><span class="sxs-lookup"><span data-stu-id="15afe-168">The transparency annotations are enforced only within an assembly.</span></span>

- <span data-ttu-id="15afe-169">Критические типы и члены должны использовать запросы компоновки для обеспечения безопасности при вызовах извне.</span><span class="sxs-lookup"><span data-stu-id="15afe-169">Security-critical types and members must use link demands to enforce security for calls from outside the assembly.</span></span>

- <span data-ttu-id="15afe-170">Правила наследования не применяются.</span><span class="sxs-lookup"><span data-stu-id="15afe-170">Inheritance rules are not enforced.</span></span>

- <span data-ttu-id="15afe-171">Прозрачный код потенциально способен выполнить вредоносные действия при его запуске с полным доверием.</span><span class="sxs-lookup"><span data-stu-id="15afe-171">The potential exists for transparent code to do harmful things when run in full trust.</span></span>

## <a name="transparency-enforcement"></a><span data-ttu-id="15afe-172">Обеспечение прозрачности</span><span class="sxs-lookup"><span data-stu-id="15afe-172">Transparency Enforcement</span></span>

<span data-ttu-id="15afe-173">Правила прозрачности не применяются до вычисления прозрачности.</span><span class="sxs-lookup"><span data-stu-id="15afe-173">Transparency rules are not enforced until transparency is calculated.</span></span> <span data-ttu-id="15afe-174">При нарушении правила прозрачности возникает исключение <xref:System.InvalidOperationException>.</span><span class="sxs-lookup"><span data-stu-id="15afe-174">At that time, an <xref:System.InvalidOperationException> is thrown if a transparency rule is violated.</span></span> <span data-ttu-id="15afe-175">Время расчета прозрачности зависит от различных факторов и не может быть предсказано заранее.</span><span class="sxs-lookup"><span data-stu-id="15afe-175">The time that transparency is calculated depends on multiple factors and cannot be predicted.</span></span> <span data-ttu-id="15afe-176">Расчет происходит в последний возможный момент.</span><span class="sxs-lookup"><span data-stu-id="15afe-176">It is calculated as late as possible.</span></span> <span data-ttu-id="15afe-177">В .NET Framework 4 Вычисление прозрачности на уровне сборки выполняется раньше, чем в .NET Framework 2,0.</span><span class="sxs-lookup"><span data-stu-id="15afe-177">In the .NET Framework 4, assembly-level transparency calculation occurs sooner than it does in the .NET Framework 2.0.</span></span> <span data-ttu-id="15afe-178">Гарантируется только то, что расчет прозрачности будет выполнен к тому моменту, когда он понадобится.</span><span class="sxs-lookup"><span data-stu-id="15afe-178">The only guarantee is that transparency calculation will occur by the time it is needed.</span></span> <span data-ttu-id="15afe-179">Этот похоже на то, как JIT-компилятор изменяет момент компиляции метода и обнаружения всех ошибок в нем.</span><span class="sxs-lookup"><span data-stu-id="15afe-179">This is similar to how the just-in-time (JIT) compiler can change the point when a method is compiled and any errors in that method are detected.</span></span> <span data-ttu-id="15afe-180">Расчет прозрачности выполняется без внешних проявлений, если в коде нет ошибок прозрачности.</span><span class="sxs-lookup"><span data-stu-id="15afe-180">Transparency calculation is invisible if your code does not have any transparency errors.</span></span>

## <a name="see-also"></a><span data-ttu-id="15afe-181">См. также раздел</span><span class="sxs-lookup"><span data-stu-id="15afe-181">See also</span></span>

- [<span data-ttu-id="15afe-182">Прозрачный для системы безопасности код, уровень 1</span><span class="sxs-lookup"><span data-stu-id="15afe-182">Security-Transparent Code, Level 1</span></span>](security-transparent-code-level-1.md)
- [<span data-ttu-id="15afe-183">Прозрачный с точки зрения безопасности код, уровень 2</span><span class="sxs-lookup"><span data-stu-id="15afe-183">Security-Transparent Code, Level 2</span></span>](security-transparent-code-level-2.md)
