---
title: Оператору Else должен предшествовать соответствующий оператор If или ElseIf.
ms.date: 07/20/2015
f1_keywords:
- vbc30028
- bc30028
helpviewer_keywords:
- BC30028
ms.assetid: c6ed34de-d6ed-4227-9880-538055aff20a
ms.openlocfilehash: fee4a48a95aeb020a0f7e3c42a29812b9a0848e9
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 06/04/2020
ms.locfileid: "84404087"
---
# <a name="else-must-be-preceded-by-a-matching-if-or-elseif"></a><span data-ttu-id="f5801-102">Оператору Else должен предшествовать соответствующий оператор If или ElseIf.</span><span class="sxs-lookup"><span data-stu-id="f5801-102">'#Else' must be preceded by a matching '#If' or '#ElseIf'</span></span>
<span data-ttu-id="f5801-103">`#Else` является директивой условной компиляции.</span><span class="sxs-lookup"><span data-stu-id="f5801-103">`#Else` is a conditional compilation directive.</span></span> <span data-ttu-id="f5801-104">Директиве `#Else` не предшествует соответствующая директива `#If` или `#ElseIf` .</span><span class="sxs-lookup"><span data-stu-id="f5801-104">An `#Else` directive is not preceded by a corresponding `#If` or `#ElseIf` directive.</span></span>  
  
 <span data-ttu-id="f5801-105">**Идентификатор ошибки:** BC30028</span><span class="sxs-lookup"><span data-stu-id="f5801-105">**Error ID:** BC30028</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="f5801-106">Исправление ошибки</span><span class="sxs-lookup"><span data-stu-id="f5801-106">To correct this error</span></span>  
  
1. <span data-ttu-id="f5801-107">Убедитесь, что предшествующая директива `#If` или `#ElseIf` не отделена от этой директивы `#Else` блоком условной компиляции или неправильно размещенной директивой `#End If`.</span><span class="sxs-lookup"><span data-stu-id="f5801-107">Check that a preceding `#If` or `#ElseIf` is not separated from this `#Else` by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
2. <span data-ttu-id="f5801-108">Убедитесь, что директиве `#Else` предшествует другая директива `#Else` .</span><span class="sxs-lookup"><span data-stu-id="f5801-108">Check that `#Else` is preceded by another `#Else` directive.</span></span> <span data-ttu-id="f5801-109">Если это так, удалите `#Else` или измените ее на `#ElseIf`.</span><span class="sxs-lookup"><span data-stu-id="f5801-109">If it is, either remove `#Else` or change it to an `#ElseIf`.</span></span>  
  
3. <span data-ttu-id="f5801-110">Если все остальное в порядке, добавьте директиву `#If` в начало блока условной компиляции.</span><span class="sxs-lookup"><span data-stu-id="f5801-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f5801-111">См. также раздел</span><span class="sxs-lookup"><span data-stu-id="f5801-111">See also</span></span>

- [<span data-ttu-id="f5801-112">Директивы #If...Then...#Else</span><span class="sxs-lookup"><span data-stu-id="f5801-112">#If...Then...#Else Directives</span></span>](../language-reference/directives/if-then-else-directives.md)
